---
import App from "@/layouts/App.astro";
import LeaderboardPage from "@/components/app/leaderboard/Page";

const userId = Astro.locals.auth?.()?.userId;
if (!userId) return Astro.redirect("/login");
---
<App title="Leaderboard — Guru Sishya">
  <LeaderboardPage client:only="react" />
</App>
