[
  {
    "topic": "Content Delivery Networks (CDN)",
    "category": "System Design",
    "cheatSheet": "# CDN Cheat Sheet\n\n## Key Metrics\n- **Cache Hit Ratio (CHR)** = Hits / Total Requests — target >90% for static assets\n- **TTFB (Time to First Byte)** — CDN edge latency vs origin latency\n- **Origin RPS** — requests per second hitting origin (lower = better)\n\n## Cache-Control Quick Reference\n```\npublic, max-age=31536000, immutable       # static assets with hash\npublic, s-maxage=60, stale-while-revalidate=3600  # HTML pages\npublic, s-maxage=30, no-store            # API (CDN caches, browser doesn't)\nprivate, no-store                         # authenticated content\n```\n\n## Invalidation Decision Tree\n1. Static asset? → Use versioned URL + immutable. No invalidation needed.\n2. Content with known relationships? → Use surrogate keys / cache tags.\n3. Emergency purge? → API invalidation.\n4. Everything else? → Short TTL + stale-while-revalidate.\n\n## Push vs Pull\n- **Pull**: edge fetches on miss. Default for all web content.\n- **Push**: pre-upload to CDN. Use for large files with predictable massive demand.\n\n## CloudFront Key Facts\n- 400+ PoPs; Origin Shield = mid-tier cache layer\n- Signed URLs/cookies for private content\n- Lambda@Edge for request/response manipulation at edge\n- Invalidation: first 1000 paths/month free, then $0.005/1000\n\n## Cloudflare Key Facts\n- Anycast network; Tiered Cache = built-in origin shield\n- Workers (V8 isolates) run at edge, <1ms cold start\n- Cache-Tag for surrogate key purging\n- R2 object storage = no egress fees\n\n## Golden Rules\n1. Fingerprint static assets — infinite TTL, zero invalidation\n2. Enable origin shield — 60-90% origin load reduction\n3. Never cache authenticated responses at CDN\n4. Monitor CHR per path, not just overall\n5. Use stale-while-revalidate to prevent thundering herd",
    "resources": [
      {
        "title": "CloudFront Developer Guide",
        "author": "AWS",
        "category": "docs",
        "justification": "Primary reference for CloudFront behaviours, cache policies, and origin shield",
        "bestFor": "Engineers deploying CloudFront",
        "estimatedTime": "3 hours",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/HowCloudFrontWorks.html"
      },
      {
        "title": "Cloudflare Learning Center — CDN",
        "author": "Cloudflare",
        "category": "docs",
        "justification": "Vendor-neutral CDN concept explanations written for practitioners",
        "bestFor": "Conceptual understanding",
        "estimatedTime": "2 hours",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://www.cloudflare.com/learning/cdn/what-is-a-cdn/"
      },
      {
        "title": "Web Performance in Action",
        "author": "Jeremy Wagner",
        "category": "books",
        "justification": "In-depth coverage of Cache-Control headers and asset delivery pipeline",
        "bestFor": "Frontend engineers optimising delivery",
        "estimatedTime": "8 hours",
        "cost": "~$35",
        "confidence": "HIGH"
      }
    ],
    "ladder": {
      "levels": [
        {
          "level": 1,
          "name": "Novice",
          "dreyfusLabel": "Novice",
          "description": "Understands what a CDN is and why websites use one",
          "observableSkills": [
            "Explains cache hit vs miss",
            "Reads Cache-Control headers",
            "Knows CDN reduces latency by serving from edge"
          ],
          "milestoneProject": {
            "title": "Static Site CDN Setup",
            "description": "Deploy a static HTML/CSS/JS site behind Cloudflare free tier; verify cache headers in DevTools",
            "estimatedHours": 2
          },
          "commonPlateaus": [
            "Confusing browser cache with CDN cache",
            "Not understanding TTL expiry"
          ],
          "estimatedHours": 4,
          "prerequisites": [
            "Basic HTTP knowledge",
            "DNS fundamentals"
          ]
        },
        {
          "level": 2,
          "name": "Advanced Beginner",
          "dreyfusLabel": "Advanced Beginner",
          "description": "Configures CDN behaviours for different content types and understands invalidation",
          "observableSkills": [
            "Sets up path-based cache behaviours",
            "Implements cache invalidation via API",
            "Explains push vs pull trade-offs"
          ],
          "milestoneProject": {
            "title": "Multi-Behaviour CloudFront Distribution",
            "description": "Create a CloudFront distribution with separate behaviours for /api/*, /static/*, /* with appropriate TTLs",
            "estimatedHours": 6
          },
          "commonPlateaus": [
            "Over-invalidating cache",
            "Forgetting Vary header impact"
          ],
          "estimatedHours": 10,
          "prerequisites": [
            "HTTP caching fundamentals",
            "Basic AWS or Cloudflare experience"
          ]
        },
        {
          "level": 3,
          "name": "Competent",
          "dreyfusLabel": "Competent",
          "description": "Architects CDN strategy for production systems including origin shield and edge logic",
          "observableSkills": [
            "Designs origin shield topology",
            "Implements surrogate key invalidation",
            "Writes edge functions for A/B testing"
          ],
          "milestoneProject": {
            "title": "Edge-Computed Personalisation",
            "description": "Use Cloudflare Workers or Lambda@Edge to serve personalised content at edge without hitting origin",
            "estimatedHours": 15
          },
          "commonPlateaus": [
            "Not accounting for cache stampede on cold start",
            "Missing geo-routing edge cases"
          ],
          "estimatedHours": 30,
          "prerequisites": [
            "CDN configuration experience",
            "Basic serverless knowledge"
          ]
        },
        {
          "level": 4,
          "name": "Proficient",
          "dreyfusLabel": "Proficient",
          "description": "Optimises CDN for cost and performance at scale; troubleshoots complex caching bugs",
          "observableSkills": [
            "Analyses CHR per path and drives improvements",
            "Implements cache stampede protection",
            "Designs multi-CDN strategy"
          ],
          "milestoneProject": {
            "title": "Cache Hit Ratio Improvement Sprint",
            "description": "Audit a production CDN, identify top cache-miss contributors, implement fixes, measure improvement",
            "estimatedHours": 20
          },
          "commonPlateaus": [
            "Single-CDN lock-in risk",
            "Cache tag sprawl causing management overhead"
          ],
          "estimatedHours": 60,
          "prerequisites": [
            "Production CDN operations experience",
            "Observability tooling familiarity"
          ]
        },
        {
          "level": 5,
          "name": "Expert",
          "dreyfusLabel": "Expert",
          "description": "Designs CDN architecture for global platforms; evaluates vendor trade-offs at enterprise scale",
          "observableSkills": [
            "Architects tiered CDN with custom origin shield topologies",
            "Designs cache-consistent invalidation",
            "Evaluates build-vs-buy for custom CDN"
          ],
          "milestoneProject": {
            "title": "Multi-CDN Failover Platform",
            "description": "Design and prototype a multi-CDN routing layer using DNS-based failover and real-time performance telemetry",
            "estimatedHours": 60
          },
          "commonPlateaus": [
            "Assuming one CDN fits all content types",
            "Underestimating invalidation consistency lag"
          ],
          "estimatedHours": 120,
          "prerequisites": [
            "Proficient CDN experience",
            "Distributed systems knowledge",
            "BGP/Anycast networking"
          ]
        }
      ]
    },
    "plan": {
      "overview": "Master CDN architecture from first principles to production-grade design: edge caching mechanics, origin shield, cache invalidation strategies, and CloudFront/Cloudflare implementations.",
      "skippedTopics": "CDN billing details, video streaming protocols (HLS/DASH internals), WAF rule syntax",
      "sessions": [
        {
          "sessionNumber": 1,
          "title": "How CDNs Work: Edge Caching and the Request Lifecycle",
          "paretoJustification": "Cache hit/miss flow and TTL mechanics cover 80% of CDN interview questions and day-to-day debugging.",
          "difficulty": "beginner",
          "estimatedMinutes": 45,
          "hookQuestion": "Netflix serves 15% of global internet traffic. How can their servers possibly handle that — and why don't your video requests actually reach Netflix's data centers?",
          "realWorldMotivation": "Every major product uses a CDN. Understanding request flow through edge nodes is essential for designing scalable systems and debugging latency issues.",
          "objectives": [
            "Explain the CDN request lifecycle from DNS lookup to content delivery",
            "Define cache hit, cache miss, and cache fill",
            "Write Cache-Control headers for static assets, HTML, and API responses",
            "Explain what a PoP is and how Anycast routing works"
          ],
          "content": "## The Problem CDNs Solve\n\nA server in us-east-1 (Virginia) serves a user in Tokyo. That round-trip is ~14,000 km — adding 150–200 ms of latency before a single byte renders. Multiply this across millions of global users and two problems compound: **high latency** for distant users and **crushing load** on a single origin.\n\nA **Content Delivery Network** solves both simultaneously by caching content at geographically distributed **Points of Presence (PoPs)** — edge servers close to users.\n\n---\n\n## The CDN Request Lifecycle\n\n```mermaid\nsequenceDiagram\n    participant U as User (Tokyo)\n    participant DNS as Anycast DNS\n    participant E as Edge PoP (Tokyo)\n    participant S as Origin Shield (US-West)\n    participant O as Origin (Virginia)\n\n    U->>DNS: resolve cdn.example.com\n    DNS-->>U: nearest PoP IP\n    U->>E: GET /logo.png\n    alt Cache HIT\n        E-->>U: 200 OK  [X-Cache: HIT]\n    else Cache MISS\n        E->>S: GET /logo.png\n        alt Shield HIT\n            S-->>E: 200 OK\n            E-->>U: 200 OK\n        else Shield MISS\n            S->>O: GET /logo.png\n            O-->>S: 200 OK + Cache-Control\n            S-->>E: 200 OK (cached)\n            E-->>U: 200 OK (cached)\n        end\n    end\n```\n\nKey response headers:\n- `X-Cache: HIT` — served from edge cache\n- `X-Cache: MISS` — edge fetched from upstream\n- `Age: 3600` — seconds the object has been cached\n- `CF-Cache-Status: HIT` — Cloudflare equivalent\n\n---\n\n## Cache-Control: The Core Directive\n\n```http\n# Static asset with content hash — cache forever\nCache-Control: public, max-age=31536000, immutable\n\n# HTML — CDN caches 60s, serve stale up to 1h while refreshing\nCache-Control: public, s-maxage=60, stale-while-revalidate=3600\n\n# API — CDN caches 30s, browser must not store\nCache-Control: public, s-maxage=30, no-store\n\n# Private/authenticated — NEVER cache at CDN\nCache-Control: private, no-store\n```\n\n`s-maxage` overrides `max-age` for shared caches (CDNs). Set `max-age=3600, s-maxage=60` and browsers cache 1 hour while the CDN refreshes every minute.\n\n---\n\n## Anycast Routing\n\nCDNs advertise the **same IP from every PoP** via BGP Anycast. Internet routing automatically directs each user's packets to the topologically nearest PoP. A user in Frankfurt might hit the Amsterdam PoP depending on ISP peering — topology, not geography, decides.\n\n---\n\n## Cache Hit Ratio\n\n```\nCache Hit Ratio = Cache Hits / Total Requests\n```\n\nAt 95% CHR, your origin handles only 5% of traffic. On 1M req/day that's 50K origin hits instead of 1M — a 20x reduction.\n\n**CHR killers:**\n- Short TTLs on rarely-changing content\n- Unbounded query strings (each `?sort=price&page=3` is a distinct cache key)\n- `Vary: User-Agent` (thousands of unique values = near-zero hit rate)\n- Cookie-based cache key variation",
          "keyTakeaways": [
            "CDNs solve latency AND origin load simultaneously via edge caching",
            "Request path: User -> Anycast DNS -> Edge PoP -> Origin Shield -> Origin",
            "s-maxage controls CDN TTL independently of browser TTL",
            "Cache Hit Ratio is the primary operational CDN metric; target >90% for static assets",
            "Content-addressed URLs allow infinite TTL and eliminate invalidation complexity"
          ],
          "activities": [
            {
              "description": "Read MDN Cache-Control docs; identify which directives apply to CDN vs browser",
              "durationMinutes": 10
            },
            {
              "description": "Inspect Cache-Control and X-Cache headers on 3 major websites in DevTools",
              "durationMinutes": 10
            },
            {
              "description": "Draw the full request lifecycle from memory for a cache miss scenario",
              "durationMinutes": 10
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 15
            }
          ],
          "resources": [
            {
              "title": "Cache-Control — MDN",
              "type": "docs",
              "url": "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control"
            },
            {
              "title": "Cloudflare: What is a CDN?",
              "type": "article",
              "url": "https://www.cloudflare.com/learning/cdn/what-is-a-cdn/"
            }
          ],
          "reviewQuestions": [
            "What is the difference between a cache HIT and MISS, and which is better for your origin?:::HIT means the CDN served from its local cache without contacting origin. MISS means it fetched from upstream. HITs are better — they reduce origin load and user latency.",
            "What does s-maxage do and how does it differ from max-age?:::s-maxage sets TTL for shared caches (CDNs/proxies) specifically. max-age applies to all caches including browsers. CDNs use s-maxage when present. This lets you set different TTLs for CDN vs browser.",
            "Why does Vary: User-Agent destroy cache hit ratio?:::The CDN creates a separate cache entry per unique Vary header value. User-Agent has thousands of distinct values, making almost every request a miss. Only vary on headers that genuinely produce different content.",
            "Describe the full request path for a cache miss on a CDN with origin shield enabled.:::User DNS resolves via Anycast to nearest PoP. Edge checks cache — miss. Forwards to origin shield. If shield hits, responds to edge. If shield also misses, shield fetches from origin, caches at both shield and edge, then responds to user."
          ],
          "successCriteria": "You can explain the full CDN request lifecycle without notes, write correct Cache-Control headers for any content type, and calculate cache hit ratio from given numbers."
        },
        {
          "sessionNumber": 2,
          "title": "Cache Invalidation, Origin Shield, and Push vs Pull CDNs",
          "paretoJustification": "Cache invalidation and origin shield design are the most common CDN system design interview topics and the most frequently misconfigured in production.",
          "difficulty": "intermediate",
          "estimatedMinutes": 50,
          "hookQuestion": "Your marketing team pushed the wrong hero image to 50 million users. It is cached with a 1-year TTL. How do you fix it in the next 5 minutes?",
          "realWorldMotivation": "Cache invalidation is famously one of the two hard problems in computer science. Getting it wrong means users see stale or incorrect content — sometimes for days.",
          "objectives": [
            "Compare TTL expiry, versioned URLs, and API invalidation strategies",
            "Explain surrogate keys (cache tags) and when to use them",
            "Design origin shield topology for a global system",
            "Choose between push and pull CDN for a given use case"
          ],
          "content": "## Cache Invalidation: Three Strategies\n\n### 1. TTL Expiry — Let It Rot\n\nSet a TTL and wait. Zero operational overhead.\n\n```\nCache-Control: public, s-maxage=3600\n```\n\n**Use when:** Slight staleness is acceptable (news feeds, product listings, scores).\n\n### 2. Versioned / Fingerprinted URLs — The Gold Standard\n\nEmbed a content hash in the filename. Changed content = changed URL = automatic cache miss.\n\n```html\n<!-- Hash in filename — webpack/Vite do this automatically -->\n<script src=\"/app.a3f9c2b.js\"></script>\n```\n\nSet `max-age=31536000, immutable`. No invalidation ever needed. **Use for all static assets in a CI/CD pipeline.**\n\n### 3. API Invalidation — Instant Purge\n\n```python\n# CloudFront\nimport boto3\ncf = boto3.client('cloudfront')\ncf.create_invalidation(\n    DistributionId='E1234567890',\n    InvalidationBatch={\n        'Paths': {'Quantity': 2, 'Items': ['/index.html', '/manifest.json']},\n        'CallerReference': 'deploy-20240322'\n    }\n)\n```\n\n```bash\n# Cloudflare\ncurl -X POST \"https://api.cloudflare.com/client/v4/zones/{zone}/purge_cache\" \\\n  -H \"Authorization: Bearer {token}\" \\\n  -d '{\"files\":[\"https://example.com/hero.jpg\"]}'\n```\n\n**Cost:** CloudFront charges $0.005/1000 paths (first 1000/month free). Wildcard purges (`/api/*`) can trigger cache storms — use sparingly.\n\n### 4. Surrogate Keys / Cache Tags\n\nTag responses with logical identifiers; purge all sharing a tag atomically.\n\n```http\n# Origin sends this response header\nSurrogate-Key: product-42 category-electronics\n# Cloudflare variant:\nCache-Tag: product-42,category-electronics\n```\n\n```bash\n# Purge everything tagged product-42 across all PoPs\ncurl -X POST \".../purge_cache\" -d '{\"tags\":[\"product-42\"]}'\n```\n\n**E-commerce use case:** Product #42 inventory changes → purge tag `product-42` → product page, category listing, and search results all refresh instantly.\n\n---\n\n## Origin Shield Architecture\n\n```mermaid\ngraph LR\n    subgraph Edge PoPs\n      E1[Tokyo]\n      E2[London]\n      E3[Sao Paulo]\n      E4[Sydney]\n    end\n    S[Origin Shield<br/>us-east-1]\n    O[Origin Server]\n\n    E1 --> S\n    E2 --> S\n    E3 --> S\n    E4 --> S\n    S --> O\n```\n\nWithout shield: every PoP cache miss → independent origin request → potentially 400 simultaneous hits for the same object.\n\nWith shield: all PoP misses route to one shield node → **one** origin request → fanned out to all waiting PoPs.\n\n**Result:** 60–90% reduction in origin load on cache misses.\n\nEnable in CloudFront via \"Origin Shield\" distribution setting (pick region nearest your origin).\nCloudflare: \"Tiered Cache\" (free tier) + Argo Smart Routing (paid).\n\n---\n\n## Push vs Pull CDN\n\n| | Pull CDN | Push CDN |\n|---|---|---|\n| Mechanism | Edge fetches from origin on first miss | You upload to CDN storage proactively |\n| First-user latency | Higher (cold start) | Zero (content pre-positioned) |\n| Best for | Web assets, dynamic content | Large files with predictable massive demand |\n| Operational burden | Low | High (must push every update) |\n| Examples | CloudFront, Cloudflare | S3 + CloudFront (push pattern), Akamai NetStorage |\n\n**Choose push CDN when:** You have a 50 GB game patch and millions of players will download it the moment it's available. Cold-start is unacceptable.\n\n---\n\n## Cache Stampede Prevention\n\nWhen a popular object's TTL expires, thousands of concurrent requests race to origin simultaneously (thundering herd).\n\n**Solution 1:** `stale-while-revalidate` — serve stale while one background request refreshes.\n```\nCache-Control: s-maxage=60, stale-while-revalidate=3600\n```\n\n**Solution 2:** Request coalescing — CDN holds concurrent misses, makes one upstream request, fans out response. Most CDNs do this automatically when origin shield is enabled.\n\n**Solution 3:** XFetch (probabilistic early expiry) — randomly refresh slightly before TTL, weighted by fetch time. Prevents synchronised expiry across PoPs.",
          "keyTakeaways": [
            "Versioned URLs + immutable TTL is the correct strategy for static assets — eliminates invalidation entirely",
            "API invalidation is for emergencies; surrogate keys are for structured content relationships",
            "Origin shield collapses all PoP misses into a single origin request — essential for high-traffic sites",
            "Pull CDN is the default; push CDN only makes sense for large files with predictable massive demand",
            "stale-while-revalidate prevents cache stampede without thundering herd"
          ],
          "activities": [
            {
              "description": "Sketch origin shield topology for a global e-commerce site with origin in us-east-1",
              "durationMinutes": 10
            },
            {
              "description": "Design a cache invalidation strategy for a CMS where publishing an article updates homepage, category, and tag pages",
              "durationMinutes": 15
            },
            {
              "description": "Write Cache-Control headers for: a product image, a homepage HTML, a stock-level API endpoint",
              "durationMinutes": 10
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 15
            }
          ],
          "resources": [
            {
              "title": "Cloudflare Cache Tag purging",
              "type": "docs",
              "url": "https://developers.cloudflare.com/cache/how-to/purge-cache/"
            },
            {
              "title": "CloudFront Origin Shield",
              "type": "docs",
              "url": "https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/origin-shield.html"
            }
          ],
          "reviewQuestions": [
            "A news site updates its homepage every 5 minutes. What Cache-Control strategy should you use?:::s-maxage=300 with stale-while-revalidate=60. CDN caches 5 minutes; serves stale for up to 60s while refreshing in background. Prevents thundering herd on expiry. HTML is not fingerprinted so infinite TTL is not safe.",
            "What are surrogate keys and give a concrete e-commerce use case?:::Surrogate keys (Cache-Tag on Cloudflare) are identifiers attached to cached responses via headers. You can purge all responses sharing a tag with one API call. Use case: when product #42 stock changes, purge tag 'product-42' to instantly refresh the product page, category listing, and search results simultaneously.",
            "Without origin shield, what happens when a cached object expires and 10,000 users request it simultaneously?:::Cache stampede / thundering herd — all PoPs miss simultaneously and each sends an independent request to origin. With 400 PoPs, origin receives ~400 concurrent requests for the same object. Origin shield collapses these into one request.",
            "When would you choose push CDN over pull CDN?:::Push CDN when: you have large files (50+ GB) like game patches or OS updates; you know in advance the content will be accessed by a massive simultaneous audience on a specific date; zero cold-start latency for the first user is critical."
          ],
          "successCriteria": "You can design a complete CDN strategy for a production system — choosing invalidation approach, origin shield placement, and explaining every trade-off in a system design interview."
        }
      ]
    },
    "quizBank": [
      {
        "question": "Which Cache-Control directive sets TTL specifically for CDN/proxy caches, overriding max-age for shared caches?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Remember",
        "options": [
          "max-age",
          "s-maxage",
          "no-cache",
          "proxy-revalidate"
        ],
        "correctAnswer": "s-maxage",
        "explanation": "s-maxage sets TTL for shared caches (CDNs, proxies) independently. CDNs use s-maxage when present, falling back to max-age. Browsers always use max-age."
      },
      {
        "question": "What is the primary benefit of an origin shield in a CDN architecture?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [
          "Encrypts traffic between CDN and origin",
          "Collapses cache miss requests from multiple PoPs into a single origin request",
          "Provides DDoS protection at layer 3",
          "Compresses responses before sending to edge nodes"
        ],
        "correctAnswer": "Collapses cache miss requests from multiple PoPs into a single origin request",
        "explanation": "Origin shield is a mid-tier cache. When multiple PoPs miss for the same object, all route to the shield which makes exactly one origin request, dramatically reducing origin load."
      },
      {
        "question": "A file is cached with max-age=31536000. Content was just updated. Without URL versioning, what is the fastest way to serve the new content?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Apply",
        "options": [
          "Wait for TTL to expire",
          "Use the CDN API to create an invalidation",
          "Change the HTTP method to POST",
          "Set Pragma: no-cache"
        ],
        "correctAnswer": "Use the CDN API to create an invalidation",
        "explanation": "API invalidation immediately purges the cached object. The next request fetches the new version from origin. This is why versioned URLs are preferred — they eliminate the need for invalidation entirely."
      },
      {
        "question": "A CDN PoP serves 1,000,000 req/day. 950,000 are cache hits, 50,000 hit origin. What is the CHR?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Apply",
        "options": [
          "95% — acceptable for static assets",
          "95% — not acceptable, should be >99%",
          "5% — not acceptable",
          "50% — borderline"
        ],
        "correctAnswer": "95% — acceptable for static assets",
        "explanation": "CHR = 950,000 / 1,000,000 = 95%. For static assets 90%+ is generally acceptable. The 5% represents cache fills from origin, which is normal."
      },
      {
        "question": "What is a cache stampede and which stale-serving directive mitigates it?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "Cache stampede: when a popular cached object's TTL expires across multiple PoPs simultaneously, causing hundreds of concurrent origin requests for the same content. Mitigation: stale-while-revalidate allows serving stale content while one background request refreshes the cache.",
        "explanation": "stale-while-revalidate=N means 'serve stale for up to N seconds while refreshing in background', serialising the revalidation request and preventing thundering herd."
      },
      {
        "question": "Explain surrogate keys and give a concrete e-commerce use case.",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Apply",
        "options": [],
        "correctAnswer": "Surrogate keys tag cached responses with logical identifiers via response headers. A single purge API call invalidates all cached objects sharing a tag. E-commerce: tag every page referencing product #42 with 'product-42'. When inventory changes, purge that tag to instantly refresh the product page, category listing, and search results.",
        "explanation": "Fastly calls these Surrogate-Key headers; Cloudflare uses Cache-Tag. CloudFront requires custom implementation."
      },
      {
        "question": "When would you choose a push CDN over a pull CDN?",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Apply",
        "options": [],
        "correctAnswer": "Push CDN when: you have large files (50+ GB) like game patches or OS updates; you know content will be accessed by a massive simultaneous audience on a specific date; zero cold-start latency for the first user is required.",
        "explanation": "Pull CDN is correct for almost all web content. Push CDN is a specialised tool for predictable massive demand on large binary files."
      },
      {
        "question": "How does Anycast routing direct users to the nearest CDN PoP?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "CDNs advertise the same IP address from every PoP via BGP Anycast. Internet routing automatically directs packets to the topologically nearest announcement of that IP. The user never selects a PoP — BGP routing does it transparently based on network topology, not geographic distance.",
        "explanation": "Anycast differs from GeoDNS, which returns different IPs based on requester location. Anycast works at the IP routing level."
      }
    ]
  },
  {
    "topic": "Rate Limiting & Throttling",
    "category": "System Design",
    "cheatSheet": "# Rate Limiting Cheat Sheet\n\n## Algorithm Comparison\n| Algorithm | Burst | Memory | Accuracy | Use Case |\n|---|---|---|---|---|\n| Token Bucket | Yes | O(1) | Exact | API rate limiting |\n| Leaky Bucket | No | O(queue) | Exact | Network traffic shaping |\n| Fixed Window | Yes | O(1) | Boundary attack risk | Simple counters |\n| Sliding Window Log | No | O(n) | Exact | Low-traffic, precision needed |\n| Sliding Window Counter | Small | O(1) | ~99.9% | Production API limiting |\n\n## Token Bucket in Redis (Lua)\n```lua\nlocal tokens = redis.call('GET', KEYS[1]) or CAPACITY\nlocal elapsed = (NOW - LAST_TS) / 1000.0\ntokens = math.min(CAPACITY, tokens + elapsed * RATE)\nif tokens >= 1 then tokens = tokens - 1; return 1\nelse return 0 end\n```\n\n## HTTP 429 Response\n```http\nHTTP/1.1 429 Too Many Requests\nX-RateLimit-Limit: 1000\nX-RateLimit-Remaining: 0\nX-RateLimit-Reset: 1711060800\nRetry-After: 60\n```\n\n## Granularity Layers\n1. IP — CDN/WAF level (anti-DDoS)\n2. API Key — gateway level\n3. User ID — application level\n4. User ID + Endpoint — most precise\n\n## Key Design Decisions (Interview)\n- Algorithm: Token bucket for APIs (burst-friendly)\n- Storage: Redis + atomic Lua scripts\n- Failure mode: fail open (availability) vs fail closed (security)\n- Headers: always return RateLimit headers even on success\n- Placement: API gateway before application servers",
    "resources": [
      {
        "title": "An Introduction to Rate Limiting",
        "author": "Stripe Engineering Blog",
        "category": "blogs",
        "justification": "Stripe explains how they built their own rate limiting system with token buckets",
        "bestFor": "Engineers building rate limiters",
        "estimatedTime": "30 min",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://stripe.com/blog/rate-limiters"
      },
      {
        "title": "Redis Rate Limiting Patterns",
        "author": "Redis",
        "category": "docs",
        "justification": "Official Redis documentation on implementing rate limiters with atomic Lua scripts",
        "bestFor": "Engineers using Redis for rate limiting",
        "estimatedTime": "1 hour",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://redis.io/glossary/rate-limiting/"
      },
      {
        "title": "Designing Distributed Systems",
        "author": "Burns, Beda, Hightower",
        "category": "books",
        "justification": "Covers distributed coordination patterns applicable to rate limiting",
        "bestFor": "Engineers building distributed systems",
        "estimatedTime": "10 hours",
        "cost": "~$40",
        "confidence": "MEDIUM"
      }
    ],
    "ladder": {
      "levels": [
        {
          "level": 1,
          "name": "Novice",
          "dreyfusLabel": "Novice",
          "description": "Understands what rate limiting is and why it is needed",
          "observableSkills": [
            "Explains 429 Too Many Requests",
            "Knows rate limiting protects against abuse and overload",
            "Reads Retry-After headers"
          ],
          "milestoneProject": {
            "title": "Simple In-Memory Rate Limiter",
            "description": "Implement a token bucket rate limiter in Python/Node with unit tests; handle 429 response correctly",
            "estimatedHours": 3
          },
          "commonPlateaus": [
            "Confusing rate limiting with authentication",
            "Not implementing Retry-After"
          ],
          "estimatedHours": 4,
          "prerequisites": [
            "Basic HTTP knowledge",
            "One programming language"
          ]
        },
        {
          "level": 2,
          "name": "Advanced Beginner",
          "dreyfusLabel": "Advanced Beginner",
          "description": "Implements rate limiting middleware and understands multiple algorithms",
          "observableSkills": [
            "Implements fixed window and sliding window counters",
            "Uses Redis for distributed state",
            "Returns correct HTTP 429 headers"
          ],
          "milestoneProject": {
            "title": "Redis-Backed API Rate Limiter",
            "description": "Build Express/FastAPI middleware using Redis sliding window counter with proper 429 responses and RateLimit headers",
            "estimatedHours": 8
          },
          "commonPlateaus": [
            "Race conditions in non-atomic Redis operations",
            "Not accounting for clock skew"
          ],
          "estimatedHours": 15,
          "prerequisites": [
            "Redis basics",
            "Middleware/interceptor patterns"
          ]
        },
        {
          "level": 3,
          "name": "Competent",
          "dreyfusLabel": "Competent",
          "description": "Designs multi-tier rate limiting for production APIs",
          "observableSkills": [
            "Designs per-tier rate limit schemas (free/pro/enterprise)",
            "Implements atomic Lua scripts in Redis",
            "Handles failure modes gracefully"
          ],
          "milestoneProject": {
            "title": "Multi-Tier API Rate Limiting Service",
            "description": "Build a rate limiting service with different limits per API key tier, atomic Redis Lua scripts, and graceful Redis failure handling",
            "estimatedHours": 20
          },
          "commonPlateaus": [
            "Single point of failure on Redis",
            "Not load testing the limiter itself"
          ],
          "estimatedHours": 40,
          "prerequisites": [
            "Redis advanced patterns",
            "API design experience"
          ]
        },
        {
          "level": 4,
          "name": "Proficient",
          "dreyfusLabel": "Proficient",
          "description": "Architects rate limiting for high-scale distributed systems",
          "observableSkills": [
            "Designs local+global hybrid limiters for performance",
            "Implements distributed rate limiting across data centers",
            "Analyses trade-offs between consistency and availability"
          ],
          "milestoneProject": {
            "title": "Global Rate Limiting at Scale",
            "description": "Design a rate limiting system that works across 3 geographic regions with <5ms overhead per request using local token buckets synced to a global Redis cluster",
            "estimatedHours": 40
          },
          "commonPlateaus": [
            "Assuming Redis is always fast enough",
            "Not accounting for network partitions"
          ],
          "estimatedHours": 80,
          "prerequisites": [
            "Distributed systems experience",
            "Redis cluster operations"
          ]
        },
        {
          "level": 5,
          "name": "Expert",
          "dreyfusLabel": "Expert",
          "description": "Designs rate limiting architecture for platforms at internet scale",
          "observableSkills": [
            "Designs cell-based rate limiting for multi-tenant platforms",
            "Evaluates GCRA and adaptive rate limiting algorithms",
            "Builds rate limiting as a separate service with SLA guarantees"
          ],
          "milestoneProject": {
            "title": "Rate Limiting Service with Adaptive Algorithms",
            "description": "Design and implement a rate limiting service using Generic Cell Rate Algorithm (GCRA) with adaptive limits based on system load, serving 100K+ API clients",
            "estimatedHours": 80
          },
          "commonPlateaus": [
            "Over-engineering for scale that does not exist yet",
            "Missing the human factors of limit communication to API consumers"
          ],
          "estimatedHours": 150,
          "prerequisites": [
            "Platform engineering experience",
            "Deep Redis/distributed cache expertise"
          ]
        }
      ]
    },
    "plan": {
      "overview": "Master rate limiting from algorithm internals to distributed production systems, covering token bucket, leaky bucket, sliding window variants, Redis-based distributed limiting, and system design interview patterns.",
      "skippedTopics": "Network-layer traffic shaping (TC qdisc), GCRA algorithm internals, specific vendor API Gateway configuration details",
      "sessions": [
        {
          "sessionNumber": 1,
          "title": "Rate Limiting Algorithms: Token Bucket, Leaky Bucket, and Sliding Windows",
          "paretoJustification": "Algorithm internals appear in every rate limiting interview question and determine which approach fits a given problem.",
          "difficulty": "intermediate",
          "estimatedMinutes": 50,
          "hookQuestion": "Your public API is being hammered by a client whose code has a bug — it sends 10,000 requests per second. How do you protect your service without blocking legitimate users?",
          "realWorldMotivation": "Rate limiting is the first line of defence for any public API. Understanding the algorithms is essential for both implementing them and discussing trade-offs in system design interviews.",
          "objectives": [
            "Implement token bucket rate limiting from scratch",
            "Explain leaky bucket and when to prefer it over token bucket",
            "Identify the boundary attack vulnerability in fixed window rate limiting",
            "Compare sliding window log vs sliding window counter for memory and accuracy"
          ],
          "content": "## Why Rate Limiting Exists\n\nWithout rate limiting, a single misbehaving client can consume all your server capacity — whether malicious (DDoS, credential stuffing) or accidental (runaway retry loop). Rate limiting protects:\n\n- **Availability**: prevent resource exhaustion\n- **Fairness**: ensure no single tenant starves others\n- **Cost**: cap unbounded API usage that maps to cloud spend\n- **Security**: slow brute-force and enumeration attacks\n\n---\n\n## Token Bucket Algorithm\n\nThe most widely deployed algorithm. Imagine a bucket that holds tokens:\n\n```\n- Bucket capacity: N tokens (burst limit)\n- Tokens replenish at rate R tokens/second\n- Each request consumes 1 token\n- If bucket is empty → request is rejected (429)\n```\n\n```python\nimport time\n\nclass TokenBucket:\n    def __init__(self, capacity: int, refill_rate: float):\n        self.capacity = capacity\n        self.tokens = capacity          # start full\n        self.refill_rate = refill_rate  # tokens per second\n        self.last_refill = time.monotonic()\n\n    def allow(self) -> bool:\n        now = time.monotonic()\n        elapsed = now - self.last_refill\n        # Add tokens earned since last check\n        self.tokens = min(\n            self.capacity,\n            self.tokens + elapsed * self.refill_rate\n        )\n        self.last_refill = now\n\n        if self.tokens >= 1:\n            self.tokens -= 1\n            return True\n        return False\n```\n\n**Properties:**\n- Allows **bursting** up to `capacity` tokens\n- Smooth average rate of `R` requests/second\n- Memory: O(1) per user — only stores `tokens` and `last_refill`\n- Used by: AWS API Gateway, Stripe, GitHub API\n\n---\n\n## Leaky Bucket Algorithm\n\nRequests enter a queue (the bucket) and are processed at a fixed rate — like water leaking from a hole. Excess requests overflow and are dropped.\n\n```mermaid\ngraph LR\n    R1[Request] --> B[Queue / Bucket<br/>capacity C]\n    R2[Request] --> B\n    R3[Request overflow] -.->|dropped| X[429]\n    B -->|fixed rate R/sec| S[Server]\n```\n\n**Properties:**\n- Outputs at **perfectly constant rate** — ideal for downstream systems with fixed capacity\n- Does NOT allow bursting — a sudden spike is immediately throttled\n- Used by: network traffic shaping, leaky bucket packet schedulers\n\n**Token Bucket vs Leaky Bucket:**\n| | Token Bucket | Leaky Bucket |\n|---|---|---|\n| Burst allowed? | Yes (up to capacity) | No |\n| Output rate | Average R/s, variable instantaneous | Constant R/s |\n| Use case | API rate limiting | Network traffic shaping |\n\n---\n\n## Fixed Window Counter\n\nDivide time into fixed windows (e.g., 1-minute intervals). Count requests per window per user.\n\n```python\n# Redis pseudocode\ndef allow_fixed_window(user_id: str, limit: int, window_seconds: int) -> bool:\n    key = f\"rl:{user_id}:{int(time.time()) // window_seconds}\"\n    count = redis.incr(key)\n    if count == 1:\n        redis.expire(key, window_seconds)\n    return count <= limit\n```\n\n**Problem: Boundary Attack**\nA user makes 100 requests at 00:59 and 100 more at 01:01. Both windows allow them, but they effectively sent 200 requests in 2 seconds — double the limit.\n\n```\nWindow 1: |-------- 0-59s --------- 100 req ---|\nWindow 2: |--- 60-119s --- 100 req ------------|\n          ^--- boundary: 200 req in 2 seconds\n```\n\n---\n\n## Sliding Window Log\n\nMaintain a sorted set of request timestamps per user. On each request:\n1. Remove timestamps older than the window\n2. Count remaining entries\n3. If count < limit, add current timestamp and allow\n\n```python\ndef allow_sliding_log(user_id: str, limit: int, window_seconds: int) -> bool:\n    now = time.time()\n    key = f\"rl:log:{user_id}\"\n    pipe = redis.pipeline()\n    pipe.zremrangebyscore(key, 0, now - window_seconds)  # remove old\n    pipe.zcard(key)                                       # count\n    pipe.zadd(key, {str(now): now})                       # add current\n    pipe.expire(key, window_seconds)\n    results = pipe.execute()\n    return results[1] < limit  # count before adding\n\n```\n\n**Properties:** Precise, no boundary attack. **Cost:** O(requests in window) memory per user — expensive at high traffic.\n\n---\n\n## Sliding Window Counter (Hybrid)\n\nApproximate sliding window using two fixed-window counters:\n\n```\nCurrent rate ≈ prev_count × (1 - elapsed/window) + curr_count\n```\n\nExample: window=60s, limit=100. At second 45 of the current window:\n- Previous window had 80 requests\n- Current window has 30 requests\n- Contribution from prev: 80 × (15/60) = 20\n- Estimated total: 20 + 30 = 50 → allowed\n\n**Best of both worlds:** O(1) memory, no boundary attack, slight approximation (~0.003% error at typical traffic).",
          "keyTakeaways": [
            "Token bucket allows bursting up to capacity; leaky bucket enforces constant output rate",
            "Fixed window counter is vulnerable to boundary attacks — users can send 2x limit across window boundaries",
            "Sliding window log is precise but O(n) memory; sliding window counter approximates it in O(1)",
            "Token bucket is the recommended algorithm for API rate limiting due to burst tolerance",
            "Rate limiting memory cost: O(1) for token/fixed window, O(requests) for sliding log"
          ],
          "activities": [
            {
              "description": "Implement token bucket in your language of choice without looking at examples",
              "durationMinutes": 20
            },
            {
              "description": "Draw the boundary attack scenario with concrete numbers to understand the vulnerability",
              "durationMinutes": 10
            },
            {
              "description": "Compare algorithms in a table: burst support, memory, accuracy, use case",
              "durationMinutes": 10
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 10
            }
          ],
          "resources": [
            {
              "title": "Stripe Rate Limiter blog post",
              "type": "article",
              "url": "https://stripe.com/blog/rate-limiters"
            },
            {
              "title": "Rate Limiting Algorithms — Wikipedia",
              "type": "article",
              "url": "https://en.wikipedia.org/wiki/Token_bucket"
            }
          ],
          "reviewQuestions": [
            "What are the four main rate limiting algorithms and their primary trade-offs?:::Token bucket (allows bursting, O(1) memory), leaky bucket (constant output rate, no bursting), fixed window counter (simple, O(1), vulnerable to boundary attack), sliding window log (precise, O(n) memory), sliding window counter (O(1), approximate but accurate).",
            "Explain the boundary attack vulnerability in fixed window rate limiting.:::A user sends N requests just before a window boundary and N more just after. Both windows count only N, but the user sent 2N requests in a very short span — effectively double the intended limit. Sliding window algorithms prevent this.",
            "Token bucket vs leaky bucket: which allows bursting and which guarantees constant output rate?:::Token bucket allows bursting up to bucket capacity (N simultaneous requests if tokens are available). Leaky bucket guarantees constant output rate R/sec regardless of input burst — excess requests are dropped or queued.",
            "Why is sliding window log expensive at high traffic, and what is the alternative?:::Sliding window log stores every request timestamp, so memory is O(requests in window) per user. At 1000 req/min per user with millions of users, this is prohibitive. Sliding window counter uses only two integers per user (O(1)) and approximates the sliding window with <0.1% error."
          ],
          "successCriteria": "You can implement token bucket from scratch, explain the boundary attack vulnerability, and choose the correct algorithm for a given use case."
        },
        {
          "sessionNumber": 2,
          "title": "Distributed Rate Limiting with Redis and System Design Patterns",
          "paretoJustification": "Distributed rate limiting is the real-world challenge — single-server solutions fail immediately in a scaled API fleet.",
          "difficulty": "advanced",
          "estimatedMinutes": 55,
          "hookQuestion": "You have 100 API servers behind a load balancer. Your rate limit is 100 req/min per user. How do you ensure the limit holds even when requests are spread across all 100 servers?",
          "realWorldMotivation": "Every production API fleet is horizontally scaled. Building a rate limiter that works correctly across dozens of servers is a common system design challenge and senior engineer competency.",
          "objectives": [
            "Design a Redis-based distributed rate limiter",
            "Explain why atomic Lua scripts are required for correctness",
            "Apply rate limiting at multiple granularity levels (IP, API key, user, endpoint)",
            "Handle Redis failure modes gracefully in production"
          ],
          "content": "## Distributed Rate Limiting: The Hard Problem\n\nA single-server rate limiter is trivial. The challenge is maintaining consistent limits across a **fleet of stateless API servers**.\n\n```mermaid\ngraph TD\n    U[User] --> LB[Load Balancer]\n    LB --> S1[API Server 1<br/>local counter: 30]\n    LB --> S2[API Server 2<br/>local counter: 25]\n    LB --> S3[API Server 3<br/>local counter: 28]\n    S1 --> R[(Redis Cluster<br/>global counter: 83)]\n    S2 --> R\n    S3 --> R\n```\n\nWithout a shared store, each server maintains its own counter. A user hitting all three servers can send 3× the limit. The solution: centralise state in Redis.\n\n---\n\n## Redis-Based Rate Limiter\n\n```python\nimport redis\nimport time\n\nr = redis.Redis()\n\ndef allow_request(user_id: str, limit: int, window: int) -> tuple[bool, dict]:\n    # Sliding window counter using Redis sorted set\n    now = time.time()\n    key = f\"rl:{user_id}\"\n\n    pipe = r.pipeline()\n    # Remove timestamps outside the window\n    pipe.zremrangebyscore(key, 0, now - window)\n    # Count remaining requests\n    pipe.zcard(key)\n    # Add current request timestamp\n    pipe.zadd(key, {f\"{now}\": now})\n    pipe.expire(key, window)\n    _, count, _, _ = pipe.execute()\n\n    allowed = count < limit\n    headers = {\n        \"X-RateLimit-Limit\": limit,\n        \"X-RateLimit-Remaining\": max(0, limit - count - 1),\n        \"X-RateLimit-Reset\": int(now) + window,\n        \"Retry-After\": window if not allowed else None,\n    }\n    return allowed, headers\n```\n\n**Atomic Lua script for token bucket in Redis (production pattern):**\n```lua\n-- KEYS[1] = rate limit key\n-- ARGV[1] = capacity, ARGV[2] = refill_rate, ARGV[3] = now (unix ms)\nlocal tokens = tonumber(redis.call('GET', KEYS[1]))\nlocal last = tonumber(redis.call('GET', KEYS[1]..':ts')) or tonumber(ARGV[3])\nlocal now = tonumber(ARGV[3])\nlocal capacity = tonumber(ARGV[1])\nlocal rate = tonumber(ARGV[2])\n\ntokens = tokens or capacity\nlocal elapsed = (now - last) / 1000.0\ntokens = math.min(capacity, tokens + elapsed * rate)\n\nif tokens >= 1 then\n    tokens = tokens - 1\n    redis.call('SET', KEYS[1], tokens)\n    redis.call('SET', KEYS[1]..':ts', now)\n    return 1  -- allowed\nelse\n    return 0  -- rejected\nend\n```\n\nLua scripts execute atomically in Redis — no race conditions, no distributed locks needed.\n\n---\n\n## Rate Limiting Layers and Granularity\n\nRate limits should be applied at multiple layers:\n\n| Layer | Scope | Tool |\n|---|---|---|\n| Network | IP address | CDN (Cloudflare, AWS WAF), nginx |\n| Application gateway | API key / user ID | Kong, API Gateway, custom middleware |\n| Service | Per-endpoint per-user | Application code + Redis |\n| Database | Query rate | Connection pool limits |\n\n**Granularity options:**\n- Per IP (anti-DDoS, but breaks NAT/proxies)\n- Per API key (accurate, but API key must be validated first)\n- Per user ID (best for authenticated endpoints)\n- Per endpoint + user (most precise: different limits for /search vs /checkout)\n\n---\n\n## Handling Limit Responses: HTTP 429\n\n```http\nHTTP/1.1 429 Too Many Requests\nX-RateLimit-Limit: 100\nX-RateLimit-Remaining: 0\nX-RateLimit-Reset: 1711060800\nRetry-After: 60\nContent-Type: application/json\n\n{\n  \"error\": \"rate_limit_exceeded\",\n  \"message\": \"Too many requests. Please retry after 60 seconds.\",\n  \"retry_after\": 60\n}\n```\n\n**Client-side best practices:**\n- Respect `Retry-After` header — exponential backoff with jitter\n- Cache responses where possible to reduce API calls\n- Use webhooks/streaming instead of polling for real-time data\n\n---\n\n## Thundering Herd on Limit Reset\n\nWhen a rate limit window resets, all throttled clients retry simultaneously. Solutions:\n\n1. **Jitter in Retry-After**: add random offset to reset timestamp\n2. **Token bucket instead of fixed window**: gradual token replenishment avoids synchronised resets\n3. **Queue with backpressure**: accept requests into a queue rather than immediately rejecting\n4. **Staggered windows per user**: hash user ID to offset their window start time\n\n---\n\n## Interview: Designing a Rate Limiter System\n\n**System design question: \"Design a rate limiter for a public API\"**\n\nKey decisions to discuss:\n1. **Algorithm**: Token bucket (recommended for API rate limiting — handles bursting naturally)\n2. **Storage**: Redis with atomic Lua scripts for consistency; local in-memory cache for performance with async sync to Redis\n3. **Granularity**: Per user ID per endpoint, different limits per tier (free/pro/enterprise)\n4. **Placement**: API gateway layer before application servers\n5. **Failure mode**: If Redis is down, fail open (allow all) or fail closed (reject all)? — depends on security requirements\n6. **Response**: Always return RateLimit headers; return 429 with Retry-After on rejection",
          "keyTakeaways": [
            "Without shared state, each API server enforces limits independently — users can send N×limit requests across N servers",
            "Lua scripts in Redis execute atomically — no race conditions, no need for distributed locks",
            "Rate limit at multiple layers: CDN (IP), API gateway (API key), application (user+endpoint)",
            "Return X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset, Retry-After on every response",
            "Failure mode choice (fail open vs closed) depends on whether availability or security takes priority"
          ],
          "activities": [
            {
              "description": "Implement Redis-based sliding window counter rate limiter with correct atomic operations",
              "durationMinutes": 25
            },
            {
              "description": "Design the rate limit schema for a SaaS API with free/pro/enterprise tiers",
              "durationMinutes": 15
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 15
            }
          ],
          "resources": [
            {
              "title": "Redis Rate Limiting patterns",
              "type": "docs",
              "url": "https://redis.io/glossary/rate-limiting/"
            },
            {
              "title": "AWS API Gateway throttling",
              "type": "docs",
              "url": "https://docs.aws.amazon.com/apigateway/latest/developerguide/api-gateway-request-throttling.html"
            }
          ],
          "reviewQuestions": [
            "Why does a local in-memory rate limiter fail in a horizontally scaled API fleet?:::Each server maintains its own independent counter. A user whose requests are distributed across N servers can send N times the limit — each server sees only its own fraction. A shared centralised store (Redis) is required for accurate global rate limiting.",
            "Why use a Lua script in Redis for rate limiting instead of a standard GET + SET?:::GET then SET is two separate commands with a race condition between them — another request could execute between them and cause counter inconsistency. Lua scripts execute atomically in Redis, so the read-modify-write is a single uninterruptible operation. No distributed locks needed.",
            "What HTTP status code and headers should a rate-limited response include?:::HTTP 429 Too Many Requests. Headers: X-RateLimit-Limit (the limit), X-RateLimit-Remaining (0 when limited), X-RateLimit-Reset (epoch when limit resets), Retry-After (seconds until client may retry).",
            "When designing a rate limiter, what failure mode should you choose if Redis goes down — fail open or fail closed?:::It depends on security requirements. Fail open (allow all requests) if the priority is availability — public APIs, non-security-critical endpoints. Fail closed (reject all) if the priority is security — authentication endpoints, financial transactions, or compliance-sensitive operations."
          ],
          "successCriteria": "You can design and implement a distributed rate limiter for a production API fleet, justify algorithm and storage choices, and articulate failure mode trade-offs."
        }
      ]
    },
    "quizBank": [
      {
        "question": "Which rate limiting algorithm allows burst traffic up to a capacity limit while enforcing an average rate?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Remember",
        "options": [
          "Leaky Bucket",
          "Fixed Window Counter",
          "Token Bucket",
          "Sliding Window Log"
        ],
        "correctAnswer": "Token Bucket",
        "explanation": "Token bucket stores tokens up to capacity. A burst can consume all tokens instantly (up to capacity), then requests are limited to the refill rate. Leaky bucket has no burst — output is constant."
      },
      {
        "question": "A fixed window rate limiter allows 100 req/min. A user sends 100 requests at 12:00:59 and 100 at 12:01:01. How many total requests did they successfully send?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Analyze",
        "options": [
          "100 (first window filled)",
          "200 (both windows accepted them)",
          "150 (averaged across windows)",
          "50 (penalty applied)"
        ],
        "correctAnswer": "200 (both windows accepted them)",
        "explanation": "This is the boundary attack. Window 1 (12:00-12:01) saw 100 requests. Window 2 (12:01-12:02) saw 100 requests. Both windows allowed their requests. The user sent 200 requests in 2 seconds while the limit is 100/minute."
      },
      {
        "question": "Why must Redis Lua scripts be used instead of separate GET + INCR commands for a rate limiter?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [
          "Lua scripts are faster than individual commands",
          "GET + INCR has a race condition between the two commands",
          "Redis does not support INCR natively",
          "Lua scripts persist across Redis restarts"
        ],
        "correctAnswer": "GET + INCR has a race condition between the two commands",
        "explanation": "Between GET (read count) and INCR (increment), another request can execute, leading to incorrect counts. Lua scripts are atomic in Redis — the entire script executes without interruption."
      },
      {
        "question": "What HTTP status code indicates a rate limit has been exceeded, and which header tells the client when to retry?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Remember",
        "options": [
          "503 Service Unavailable + Retry-After",
          "429 Too Many Requests + Retry-After",
          "429 Too Many Requests + X-RateLimit-Reset",
          "503 + X-Throttle-Retry"
        ],
        "correctAnswer": "429 Too Many Requests + Retry-After",
        "explanation": "HTTP 429 Too Many Requests is the correct status for rate limiting. Retry-After tells the client how many seconds to wait. X-RateLimit-Reset gives the epoch timestamp when the window resets (both are commonly returned together)."
      },
      {
        "question": "Your rate limiter Redis node goes down. Should you fail open (allow all requests) or fail closed (reject all)? What determines the right answer?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Evaluate",
        "options": [],
        "correctAnswer": "It depends on security requirements. Fail open for: public content APIs, non-security-critical endpoints, when availability SLA > security concern. Fail closed for: authentication endpoints, financial transactions, security-critical operations where bypassing limits creates risk. Most production systems fail open to maintain availability, but with monitoring and alerting.",
        "explanation": "There is no universally correct answer — it is a deliberate architectural trade-off between availability and security that must be made per-endpoint."
      },
      {
        "question": "Explain the difference between token bucket and leaky bucket algorithms and name a use case for each.",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "Token bucket: requests consume tokens from a bucket that refills at rate R. Allows bursting up to bucket capacity. Use case: API rate limiting where clients need burst capacity for batch operations. Leaky bucket: requests enter a queue and are processed at constant rate R. No burst tolerance. Use case: network traffic shaping where downstream systems need constant input rate.",
        "explanation": "Token bucket is preferred for API rate limiting. Leaky bucket is preferred for network QoS where constant output rate protects downstream capacity-constrained systems."
      },
      {
        "question": "How does a sliding window counter achieve O(1) memory while avoiding the boundary attack of fixed window?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "It uses two fixed-window counters (previous and current window) and estimates the request count as: prev_count × (1 - elapsed/window) + curr_count. This approximates a sliding window without storing individual timestamps. Memory is O(1) per user (just two counters). The approximation error is typically <0.003% at realistic traffic patterns.",
        "explanation": "The key insight is that the previous window's contribution decays linearly as the window slides forward, which can be computed from just two counters."
      }
    ]
  },
  {
    "topic": "Authentication & Authorization",
    "category": "System Design",
    "cheatSheet": "# Auth Cheat Sheet\n\n## OAuth 2.0 Grant Types\n| Grant | Use Case |\n|---|---|\n| Authorization Code + PKCE | Web/SPA/Mobile apps |\n| Client Credentials | Machine-to-machine |\n| Device Code | TV/CLI without browser |\n\n## JWT Security Checklist\n- [ ] RS256 not HS256 (asymmetric keys)\n- [ ] exp validated on every request\n- [ ] Short TTL: 15 min access, 7 days refresh\n- [ ] jti claim for revocation support\n- [ ] Never secrets in payload (base64-encoded, not encrypted)\n- [ ] Whitelist algorithms (reject alg:none)\n\n## OIDC Token Types\n- id_token: identity (JWT with sub, email, name)\n- access_token: API access\n- refresh_token: get new access tokens\n\n## RBAC Quick Pattern\n```python\nROLES = {\"viewer\": {\"read\"}, \"editor\": {\"read\",\"write\"}, \"admin\": {\"*\"}}\ndef can(role, perm): return \"*\" in ROLES[role] or perm in ROLES[role]\n```\n\n## mTLS vs Standard TLS\n- TLS: client verifies server cert\n- mTLS: both verify each other's cert\n- Use in: service mesh (Istio), zero-trust, financial APIs\n\n## Zero-Trust Principles\n1. Never trust, always verify\n2. Least privilege access\n3. Assume breach — encrypt everything, segment access\n\n## Common Vulnerabilities\n- alg:none → whitelist algorithms\n- Weak HS256 secret → use RS256\n- CSRF → SameSite=Strict + CSRF token\n- redirect_uri hijack → exact match, validate state",
    "resources": [
      {
        "title": "OAuth 2.0 RFC 6749",
        "author": "IETF",
        "category": "docs",
        "justification": "The canonical specification for OAuth 2.0",
        "bestFor": "Engineers implementing OAuth 2.0 flows",
        "estimatedTime": "3 hours",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://www.rfc-editor.org/rfc/rfc6749"
      },
      {
        "title": "jwt.io",
        "author": "Auth0",
        "category": "interactive",
        "justification": "Interactive JWT decoder and verifier with library listings for every language",
        "bestFor": "Understanding JWT structure and debugging tokens",
        "estimatedTime": "30 min",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://jwt.io"
      },
      {
        "title": "The Web Application Hacker's Handbook",
        "author": "Stuttard & Pinto",
        "category": "books",
        "justification": "Comprehensive coverage of web auth vulnerabilities including OAuth, JWT, CSRF",
        "bestFor": "Security-focused engineers",
        "estimatedTime": "15 hours",
        "cost": "~$45",
        "confidence": "HIGH"
      }
    ],
    "ladder": {
      "levels": [
        {
          "level": 1,
          "name": "Novice",
          "dreyfusLabel": "Novice",
          "description": "Understands the difference between authentication and authorization",
          "observableSkills": [
            "Explains authn vs authz",
            "Reads JWTs using jwt.io",
            "Knows what OAuth 2.0 is for"
          ],
          "milestoneProject": {
            "title": "Secure Login with JWT",
            "description": "Build a simple login API that issues a JWT and a protected endpoint that validates it",
            "estimatedHours": 4
          },
          "commonPlateaus": [
            "Storing passwords in plain text",
            "Confusing OAuth tokens with session cookies"
          ],
          "estimatedHours": 5,
          "prerequisites": [
            "Basic HTTP knowledge",
            "One backend language"
          ]
        },
        {
          "level": 2,
          "name": "Advanced Beginner",
          "dreyfusLabel": "Advanced Beginner",
          "description": "Implements OAuth 2.0 Authorization Code flow and validates JWTs correctly",
          "observableSkills": [
            "Implements OAuth 2.0 login with a real provider",
            "Validates JWT exp, iss, aud claims",
            "Implements basic RBAC middleware"
          ],
          "milestoneProject": {
            "title": "OAuth 2.0 Login + RBAC",
            "description": "Implement Google/GitHub OAuth 2.0 login with PKCE, JWT validation, and role-based route protection",
            "estimatedHours": 10
          },
          "commonPlateaus": [
            "Not validating redirect_uri exactly",
            "Storing refresh tokens insecurely"
          ],
          "estimatedHours": 20,
          "prerequisites": [
            "HTTP session management",
            "Basic security awareness"
          ]
        },
        {
          "level": 3,
          "name": "Competent",
          "dreyfusLabel": "Competent",
          "description": "Designs auth systems for multi-service architectures",
          "observableSkills": [
            "Designs token refresh flow with rotation",
            "Implements mTLS for service-to-service auth",
            "Chooses between JWT and opaque tokens based on requirements"
          ],
          "milestoneProject": {
            "title": "Multi-Service Auth Architecture",
            "description": "Design auth for a 3-service system: user service issues JWTs, two downstream services validate them; implement refresh token rotation",
            "estimatedHours": 25
          },
          "commonPlateaus": [
            "JWT revocation is harder than expected",
            "mTLS certificate management complexity"
          ],
          "estimatedHours": 50,
          "prerequisites": [
            "OAuth 2.0 implementation experience",
            "Basic PKI knowledge"
          ]
        },
        {
          "level": 4,
          "name": "Proficient",
          "dreyfusLabel": "Proficient",
          "description": "Architects auth for large-scale distributed systems with zero-trust principles",
          "observableSkills": [
            "Designs zero-trust network access patterns",
            "Implements fine-grained ABAC with OPA",
            "Designs SSO across multiple applications"
          ],
          "milestoneProject": {
            "title": "Zero-Trust Auth Platform",
            "description": "Design and implement OIDC-based SSO across 5 internal applications with OPA policy enforcement and mTLS service mesh",
            "estimatedHours": 50
          },
          "commonPlateaus": [
            "Over-complicating simple auth requirements",
            "Certificate rotation operations"
          ],
          "estimatedHours": 100,
          "prerequisites": [
            "Production auth system experience",
            "Service mesh knowledge"
          ]
        },
        {
          "level": 5,
          "name": "Expert",
          "dreyfusLabel": "Expert",
          "description": "Designs identity and access management platforms for enterprise and internet scale",
          "observableSkills": [
            "Designs federated identity across organisations",
            "Architects CIAM (Customer Identity and Access Management) at scale",
            "Evaluates auth vendor trade-offs for enterprise compliance"
          ],
          "milestoneProject": {
            "title": "Enterprise Identity Federation",
            "description": "Design a federated identity system connecting 3 organisations' IdPs with cross-org RBAC and audit logging meeting SOC2/ISO27001 requirements",
            "estimatedHours": 100
          },
          "commonPlateaus": [
            "Compliance requirements conflicting with UX",
            "Cross-org trust establishment complexity"
          ],
          "estimatedHours": 200,
          "prerequisites": [
            "Enterprise auth platform experience",
            "Security compliance knowledge"
          ]
        }
      ]
    },
    "plan": {
      "overview": "Master authentication and authorization from OAuth 2.0 flows to production security patterns, covering JWT, SSO, RBAC, mTLS, and zero-trust architecture.",
      "skippedTopics": "SAML 2.0 XML details, Kerberos, hardware security keys (FIDO2/WebAuthn internals), specific vendor IAM products",
      "sessions": [
        {
          "sessionNumber": 1,
          "title": "OAuth 2.0, JWT, and SSO: Identity Protocols Demystified",
          "paretoJustification": "OAuth 2.0 and JWT appear in virtually every backend system design discussion and are common interview deep-dives.",
          "difficulty": "intermediate",
          "estimatedMinutes": 55,
          "hookQuestion": "How does clicking 'Sign in with Google' on a third-party app grant that app access to your Google Calendar without giving it your Google password?",
          "realWorldMotivation": "Every modern application uses OAuth 2.0 and JWTs. Misunderstanding these protocols leads to security vulnerabilities that are routinely exploited in production systems.",
          "objectives": [
            "Explain OAuth 2.0 Authorization Code flow with PKCE",
            "Decode and validate a JWT, explaining each claim",
            "Distinguish between OAuth 2.0 (authorization) and OIDC (authentication)",
            "Explain SSO session mechanics across multiple applications"
          ],
          "content": "## Authentication vs Authorization\n\nThese terms are frequently conflated in interviews:\n\n- **Authentication (AuthN)**: Who are you? Verify identity. (Login, API key check)\n- **Authorization (AuthZ)**: What can you do? Verify permissions. (RBAC, policy check)\n\nAuthentication always precedes authorization. You cannot authorize an unknown identity.\n\n---\n\n## OAuth 2.0: The Delegation Framework\n\nOAuth 2.0 is not an authentication protocol — it is an **authorization delegation framework**. It lets users grant third-party apps limited access to their resources without sharing passwords.\n\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant C as Client App\n    participant AS as Auth Server\n    participant RS as Resource Server (API)\n\n    U->>C: Click \"Login with Google\"\n    C->>AS: Authorization Request<br/>(client_id, scope, redirect_uri, state)\n    AS->>U: Login + Consent Screen\n    U->>AS: Approve\n    AS->>C: Authorization Code (via redirect)\n    C->>AS: Exchange code for tokens<br/>(code + client_secret)\n    AS->>C: Access Token + Refresh Token\n    C->>RS: API Request + Bearer Token\n    RS->>RS: Validate token\n    RS->>C: Protected Resource\n```\n\n**Four OAuth 2.0 Grant Types:**\n\n| Grant Type | Use Case |\n|---|---|\n| Authorization Code | Web apps with backend (most secure, use PKCE for SPAs) |\n| Client Credentials | Machine-to-machine (no user involved) |\n| Device Code | TV/CLI apps without browser |\n| ~~Implicit~~ | Deprecated — replaced by Auth Code + PKCE |\n\n**PKCE (Proof Key for Code Exchange):** Required for public clients (SPAs, mobile). Client generates a `code_verifier` (random), hashes it to `code_challenge`, sends challenge with auth request, and verifier with token exchange. Prevents authorization code interception.\n\n---\n\n## JWT: JSON Web Tokens\n\nA JWT is a self-contained, signed token. The resource server validates it **without calling the auth server**.\n\n```\nHeader.Payload.Signature\neyJhbGciOiJSUzI1NiJ9.eyJzdWIiOiJ1c2VyXzEyMyIsInJvbGUiOiJhZG1pbiIsImV4cCI6MTcxMTA2MDgwMH0.SIG\n```\n\n```python\nimport jwt  # PyJWT\n\n# Create (auth server side)\ntoken = jwt.encode(\n    {\"sub\": \"user_123\", \"role\": \"admin\", \"exp\": time.time() + 3600},\n    private_key,\n    algorithm=\"RS256\"\n)\n\n# Verify (resource server side — no auth server call needed)\npayload = jwt.decode(token, public_key, algorithms=[\"RS256\"])\nuser_id = payload[\"sub\"]\nrole = payload[\"role\"]\n```\n\n**JWT Security Rules:**\n1. **Never store secrets in payload** — it is base64-encoded, not encrypted\n2. **Always validate `exp`** — expired tokens must be rejected\n3. **Prefer RS256 over HS256** — asymmetric signature allows public key distribution without sharing secrets\n4. **Keep JWTs short-lived** — 15 minutes for access tokens; use refresh tokens for longer sessions\n5. **Implement token revocation** via a denylist or short TTL + refresh token rotation\n\n**JWT vs Opaque Tokens:**\n\n| | JWT | Opaque Token |\n|---|---|---|\n| Validation | Local (no network call) | Auth server lookup (network call) |\n| Revocation | Difficult (must wait for exp) | Immediate (delete from store) |\n| Size | ~500 bytes | ~32 bytes |\n| Use case | Stateless microservices | When immediate revocation is required |\n\n---\n\n## SSO: Single Sign-On\n\nSSO lets users authenticate once and access multiple applications. Two main protocols:\n\n**SAML 2.0** — Enterprise/legacy. XML-based assertions. Identity Provider (IdP) issues signed XML tokens to Service Providers (SP). Okta, Azure AD, Google Workspace.\n\n**OpenID Connect (OIDC)** — Modern. Built on OAuth 2.0. Adds `id_token` (JWT) for identity. Separates authentication (OIDC) from authorization (OAuth 2.0).\n\n```\nOIDC Token Types:\n- id_token: Who the user is (JWT with name, email, sub)\n- access_token: What the user can do (pass to APIs)\n- refresh_token: Get new access tokens without re-login\n```\n\n**SSO Session Flow:**\n1. User hits App A — no session\n2. App A redirects to IdP\n3. User authenticates at IdP — IdP creates SSO session cookie\n4. IdP redirects back with token\n5. User hits App B — no session\n6. App B redirects to IdP — IdP sees SSO cookie, auto-issues token\n7. User is logged into App B without re-entering credentials",
          "keyTakeaways": [
            "OAuth 2.0 is authorization delegation, not authentication — OIDC adds authentication on top",
            "Authorization Code + PKCE is the correct grant type for SPAs and mobile apps",
            "JWT payloads are base64-encoded not encrypted — never store secrets in claims",
            "RS256 (asymmetric) is preferred over HS256 (symmetric) for JWTs in distributed systems",
            "Short-lived access tokens (15 min) with refresh token rotation minimise theft impact"
          ],
          "activities": [
            {
              "description": "Decode a real JWT at jwt.io and identify sub, exp, iat, and aud claims",
              "durationMinutes": 10
            },
            {
              "description": "Draw the OAuth 2.0 Authorization Code + PKCE flow from memory",
              "durationMinutes": 15
            },
            {
              "description": "Explain SSO flow to someone unfamiliar — use Google login across YouTube and Gmail as example",
              "durationMinutes": 10
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 20
            }
          ],
          "resources": [
            {
              "title": "jwt.io interactive decoder",
              "type": "interactive",
              "url": "https://jwt.io"
            },
            {
              "title": "OAuth 2.0 Playground",
              "type": "interactive",
              "url": "https://www.oauth.com/playground/"
            }
          ],
          "reviewQuestions": [
            "What is the difference between authentication and authorization?:::Authentication (AuthN) verifies identity — who are you? Authorization (AuthZ) verifies permissions — what can you do? Authentication always precedes authorization.",
            "What is OAuth 2.0 and why is it NOT an authentication protocol?:::OAuth 2.0 is an authorization delegation framework — it lets users grant third-party apps limited access to their resources. It does not define how identity is verified (that is OpenID Connect built on top of it). The access token tells you what the holder can do, not who they are.",
            "Why is the Authorization Code grant type preferred over the implicit grant for SPAs?:::Authorization Code + PKCE is more secure because the access token is never exposed in the browser URL (redirect fragment). The implicit flow put tokens in the URL where they could be captured in browser history, referrer headers, or by malicious scripts. PKCE prevents authorization code interception by binding the code to a verifier only the legitimate client knows.",
            "What are the security implications of using HS256 vs RS256 for JWT signing?:::HS256 uses a shared symmetric secret — every service that verifies tokens must know the secret, creating many attack surfaces. RS256 uses asymmetric keys: the auth server signs with a private key, and any service can verify using the public key (distributable safely). Compromise of one verifier does not expose the signing key."
          ],
          "successCriteria": "You can explain the complete OAuth 2.0 Authorization Code + PKCE flow, decode and validate a JWT, and articulate why OIDC is needed on top of OAuth 2.0."
        },
        {
          "sessionNumber": 2,
          "title": "RBAC, mTLS, and Zero-Trust Security Architecture",
          "paretoJustification": "RBAC, mTLS, and zero-trust are standard requirements in senior system design interviews for any security-sensitive system.",
          "difficulty": "advanced",
          "estimatedMinutes": 55,
          "hookQuestion": "Your company has 50 microservices. How do you ensure that only Service A can call Service B — and that this is enforced cryptographically, not just by network firewall rules?",
          "realWorldMotivation": "Zero-trust is the modern security baseline. Understanding mTLS and RBAC is essential for designing systems that pass security review at any company with serious compliance requirements.",
          "objectives": [
            "Implement RBAC and explain when to use ABAC instead",
            "Explain mTLS handshake and when to use it",
            "Apply zero-trust principles to a microservice architecture",
            "Identify and mitigate common JWT and OAuth vulnerabilities"
          ],
          "content": "## RBAC: Role-Based Access Control\n\nUsers are assigned **roles**, and roles have **permissions**. Authorization checks role membership.\n\n```python\n# Role definitions\nROLES = {\n    \"viewer\":    {\"read:posts\", \"read:comments\"},\n    \"editor\":    {\"read:posts\", \"write:posts\", \"read:comments\", \"write:comments\"},\n    \"admin\":     {\"*\"},  # all permissions\n}\n\n# User → Role mapping (from DB or JWT claim)\ndef has_permission(user_role: str, required_permission: str) -> bool:\n    permissions = ROLES.get(user_role, set())\n    return \"*\" in permissions or required_permission in permissions\n\n# In a FastAPI route:\n@app.delete(\"/posts/{id}\")\nasync def delete_post(id: int, user=Depends(get_current_user)):\n    if not has_permission(user.role, \"write:posts\"):\n        raise HTTPException(403, \"Insufficient permissions\")\n    ...\n```\n\n**RBAC vs ABAC (Attribute-Based Access Control):**\n- RBAC: simple, fast, but coarse-grained (\"admin can do everything\")\n- ABAC: fine-grained policies based on attributes (user, resource, environment): \"editor can edit posts where post.author == user.id AND current_time is business_hours\"\n- Tools: OPA (Open Policy Agent) for ABAC/policy-as-code\n\n---\n\n## mTLS: Mutual TLS for Service-to-Service Auth\n\nIn standard TLS, only the server presents a certificate. In **mTLS**, both client and server present certificates, giving cryptographic mutual authentication.\n\n```mermaid\nsequenceDiagram\n    participant C as Service A (Client)\n    participant S as Service B (Server)\n\n    C->>S: ClientHello\n    S->>C: ServerHello + Server Certificate\n    C->>S: Client Certificate\n    S->>C: Verify Client Cert (CA check)\n    C->>S: Verify Server Cert (CA check)\n    Note over C,S: Mutual authentication complete\n    C->>S: Encrypted API Request\n    S->>C: Encrypted Response\n```\n\n**mTLS use cases:**\n- Service mesh (Istio, Linkerd) — automatic mTLS between all pods\n- Zero-trust network architecture\n- Financial APIs (banking, payments) requiring client identity proof\n\n**Certificate management:**\n```bash\n# Generate client cert with your internal CA\nopenssl req -new -newkey rsa:2048 -keyout client.key -out client.csr -subj \"/CN=service-a\"\nopenssl x509 -req -in client.csr -CA ca.crt -CAkey ca.key -out client.crt -days 365\n\n# Use in curl\ncurl --cert client.crt --key client.key https://service-b:8443/api/resource\n```\n\n---\n\n## Zero-Trust Architecture\n\nTraditional security: \"trust everything inside the network perimeter.\"\nZero-trust: **\"never trust, always verify\"** — treat every request as potentially hostile regardless of network location.\n\n**Zero-trust pillars:**\n1. **Verify explicitly**: Always authenticate and authorize based on all available signals (identity, device health, location, time)\n2. **Least privilege access**: Grant minimum permissions required; time-limited where possible\n3. **Assume breach**: Design assuming the network is already compromised; encrypt everything, segment access\n\n**Implementation patterns:**\n- mTLS for all service-to-service communication\n- Short-lived tokens (no long-lived API keys)\n- Continuous authorization (re-verify on every request, not just login)\n- Device posture checks (is the device patched? enrolled in MDM?)\n\n---\n\n## JWT Token Security in Practice\n\n```python\n# Secure JWT configuration\nfrom datetime import datetime, timedelta, timezone\nimport jwt\n\ndef create_access_token(user_id: str, roles: list[str]) -> str:\n    now = datetime.now(timezone.utc)\n    payload = {\n        \"sub\": user_id,\n        \"roles\": roles,\n        \"iat\": now,\n        \"exp\": now + timedelta(minutes=15),  # SHORT-lived\n        \"jti\": secrets.token_hex(16),        # unique token ID for revocation\n    }\n    return jwt.encode(payload, PRIVATE_KEY, algorithm=\"RS256\")\n\ndef verify_token(token: str) -> dict:\n    try:\n        payload = jwt.decode(token, PUBLIC_KEY, algorithms=[\"RS256\"])\n        # Check token revocation denylist\n        if redis.sismember(\"revoked_tokens\", payload[\"jti\"]):\n            raise ValueError(\"Token has been revoked\")\n        return payload\n    except jwt.ExpiredSignatureError:\n        raise HTTPException(401, \"Token expired\")\n    except jwt.InvalidTokenError:\n        raise HTTPException(401, \"Invalid token\")\n```\n\n**Refresh Token Rotation:**\n1. Access token: 15 minutes, stateless JWT\n2. Refresh token: 7 days, opaque, stored in DB\n3. On refresh: issue new access token AND new refresh token; invalidate old refresh token\n4. If old refresh token is reused → token theft detected → revoke all user sessions\n\n---\n\n## Common Auth Vulnerabilities (Interview Points)\n\n| Vulnerability | Description | Mitigation |\n|---|---|---|\n| JWT alg:none | Attacker removes signature, sets alg=none | Reject tokens with alg=none; whitelist algorithms |\n| JWT HS256 secret brute-force | Weak shared secret guessed offline | Use RS256; enforce strong secrets; rotate regularly |\n| CSRF | Attacker tricks browser to make authenticated request | SameSite=Strict cookies; CSRF tokens; Origin header check |\n| OAuth redirect_uri hijack | Attacker registers malicious redirect URI | Exact URI matching (no wildcards); validate state parameter |\n| Token leakage in logs | Access tokens in URL query strings appear in logs | Always use Authorization header, never query params |",
          "keyTakeaways": [
            "RBAC is role-based; ABAC adds attribute context for fine-grained policies — use OPA for ABAC",
            "mTLS provides cryptographic mutual authentication; standard TLS only authenticates the server",
            "Zero-trust: never trust, always verify, least privilege, assume breach",
            "Refresh token rotation detects theft: reuse of an already-rotated token triggers full session revocation",
            "JWT alg:none is a critical vulnerability — always whitelist allowed algorithms"
          ],
          "activities": [
            {
              "description": "Implement RBAC middleware that reads roles from JWT claims and enforces permissions on routes",
              "durationMinutes": 20
            },
            {
              "description": "Sketch zero-trust architecture for a 5-service system with mTLS and a central auth service",
              "durationMinutes": 20
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 15
            }
          ],
          "resources": [
            {
              "title": "Open Policy Agent (OPA)",
              "type": "docs",
              "url": "https://www.openpolicyagent.org/docs/latest/"
            },
            {
              "title": "NIST Zero Trust Architecture SP 800-207",
              "type": "docs",
              "url": "https://csrc.nist.gov/publications/detail/sp/800-207/final"
            }
          ],
          "reviewQuestions": [
            "What is RBAC and when would you use ABAC instead?:::RBAC assigns permissions to roles and users to roles. Simple and fast. Use ABAC when RBAC becomes too coarse-grained — when access decisions require evaluating resource attributes, user attributes, and environmental context together (e.g., 'an editor can edit their own posts during business hours from an approved device').",
            "What is mTLS and how does it differ from standard TLS?:::Standard TLS: server presents certificate; client verifies server identity. mTLS: both server and client present certificates; each verifies the other's identity. This provides cryptographic mutual authentication, proving both parties are who they claim to be. Used in service meshes (Istio) and zero-trust architectures.",
            "Explain the 'alg:none' JWT vulnerability.:::Some JWT libraries allow an attacker to craft a token with the header algorithm set to 'none' and no signature. A vulnerable library skips signature verification for alg=none and accepts the forged token. Mitigation: always whitelist allowed algorithms (e.g., ['RS256']) and reject anything else, including 'none'.",
            "What is refresh token rotation and why does it help detect token theft?:::On each refresh: issue a new access token AND a new refresh token, invalidate the old refresh token. If an attacker steals a refresh token and uses it after the legitimate user already refreshed, the stolen token is already invalidated — triggering a 'refresh token reuse' alert. The system then revokes all user sessions, limiting attacker access window."
          ],
          "successCriteria": "You can design an auth architecture for a multi-service system using RBAC, mTLS, and zero-trust principles, and identify the top 5 JWT/OAuth vulnerabilities with their mitigations."
        }
      ]
    },
    "quizBank": [
      {
        "question": "What is the difference between OAuth 2.0 and OpenID Connect (OIDC)?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [
          "They are the same protocol with different names",
          "OAuth 2.0 handles authorization delegation; OIDC adds authentication (identity) on top of OAuth 2.0",
          "OIDC is for mobile apps; OAuth 2.0 is for web apps",
          "OAuth 2.0 uses JWTs; OIDC uses opaque tokens"
        ],
        "correctAnswer": "OAuth 2.0 handles authorization delegation; OIDC adds authentication (identity) on top of OAuth 2.0",
        "explanation": "OAuth 2.0 lets users delegate access (what the app can do). OIDC extends it with an id_token (JWT) that identifies who the user is. You need OIDC when you want to know the user's identity, not just grant access."
      },
      {
        "question": "Why is Authorization Code + PKCE preferred over the Implicit grant for Single Page Applications?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [
          "It requires fewer round trips",
          "The Implicit flow exposes access tokens in the URL where they can be captured; PKCE prevents code interception",
          "PKCE works offline; Implicit requires an internet connection",
          "Authorization Code generates shorter tokens"
        ],
        "correctAnswer": "The Implicit flow exposes access tokens in the URL where they can be captured; PKCE prevents code interception",
        "explanation": "Implicit flow put tokens in the redirect URL fragment — visible in browser history, referrer headers, and server logs. Authorization Code + PKCE keeps tokens out of the URL and binds the code to a verifier only the legitimate client knows."
      },
      {
        "question": "A JWT uses HS256 (HMAC-SHA256). What is the main security risk of this choice in a multi-service architecture?",
        "format": "multiple-choice",
        "difficulty": 4,
        "bloomLabel": "Analyze",
        "options": [
          "HS256 tokens are larger than RS256 tokens",
          "Every service that verifies tokens must share the same secret, increasing the attack surface",
          "HS256 does not support the exp claim",
          "HS256 tokens cannot be decoded at jwt.io"
        ],
        "correctAnswer": "Every service that verifies tokens must share the same secret, increasing the attack surface",
        "explanation": "HS256 uses a symmetric secret. Every service needing to verify tokens must know the secret — compromise of any one service exposes the signing key. RS256 uses asymmetric keys: only the auth server needs the private key; verifiers use the public key (safely distributable)."
      },
      {
        "question": "What is the alg:none JWT vulnerability and how do you prevent it?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Remember",
        "options": [
          "An attacker removes the signature and sets alg to none; some libraries skip verification for alg=none",
          "An attacker replaces RS256 with HS256 and uses the public key as the HMAC secret",
          "An attacker sets exp to none to create a non-expiring token",
          "An attacker removes the payload to create an empty token"
        ],
        "correctAnswer": "An attacker removes the signature and sets alg to none; some libraries skip verification for alg=none",
        "explanation": "alg:none: attacker crafts a token with algorithm=none and removes the signature. Vulnerable libraries accept it. Prevention: whitelist allowed algorithms in your JWT library configuration (e.g., algorithms=['RS256']); never accept alg=none."
      },
      {
        "question": "Explain refresh token rotation and how it detects token theft.",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "On every refresh: issue a new access token AND a new refresh token; immediately invalidate the old refresh token. If an attacker steals a refresh token and uses it after the legitimate user has already refreshed, the stolen token is already invalidated. The system detects this reuse as a token theft signal and can revoke all sessions for that user.",
        "explanation": "Refresh token rotation limits the window of attacker access even if a refresh token is stolen. Without rotation, a stolen refresh token provides indefinite access until the user changes their password."
      },
      {
        "question": "What is mTLS and how does it differ from standard TLS?",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "Standard TLS: server presents a certificate; client verifies server identity. The client is anonymous to the server cryptographically. mTLS (Mutual TLS): both client and server present certificates; each verifies the other. This provides cryptographic proof of client identity, not just server identity. Used in service meshes (Istio), zero-trust architectures, and financial APIs.",
        "explanation": "mTLS eliminates the need for API keys or bearer tokens for service-to-service auth — identity is proved by the TLS certificate itself."
      },
      {
        "question": "What are the three pillars of zero-trust architecture?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Remember",
        "options": [
          "Encrypt, Monitor, Patch",
          "Never trust always verify; Least privilege; Assume breach",
          "Authenticate, Authorize, Audit",
          "Perimeter, Detection, Response"
        ],
        "correctAnswer": "Never trust always verify; Least privilege; Assume breach",
        "explanation": "NIST SP 800-207 defines zero-trust around: (1) verify explicitly — authenticate and authorize using all available signals; (2) use least privilege access — minimum permissions, time-limited; (3) assume breach — design as if the network is already compromised."
      },
      {
        "question": "When should you use ABAC instead of RBAC for authorization?",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Apply",
        "options": [],
        "correctAnswer": "Use ABAC when RBAC becomes too coarse-grained for your access control requirements. RBAC works well when roles map cleanly to permission sets. ABAC is needed when access decisions require evaluating multiple attributes: user attributes (department, clearance), resource attributes (owner, classification), and environmental attributes (time of day, device posture). Example: 'editors can only edit posts they authored, during business hours, from approved devices' — this cannot be expressed in RBAC alone.",
        "explanation": "OPA (Open Policy Agent) is the standard tool for implementing ABAC / policy-as-code in microservice architectures."
      }
    ]
  },
  {
    "topic": "Monitoring & Observability",
    "category": "System Design",
    "cheatSheet": "# Monitoring & Observability Cheat Sheet\n\n## Three Pillars\n- **Metrics**: what is happening (Prometheus, numeric, cheap)\n- **Logs**: what happened (ELK, structured JSON, rich context)\n- **Traces**: where it happened (Jaeger/OTel, cross-service, timing)\n\n## Four Golden Signals\n| Signal | Alert Threshold Example |\n|---|---|\n| Latency | p99 > 500ms for 5min |\n| Traffic | RPS drops 50% suddenly |\n| Errors | 5xx rate > 1% for 2min |\n| Saturation | CPU > 90% for 10min |\n\n## SLI / SLO / SLA\n```\nSLI: fraction of requests < 200ms\nSLO: 99% of requests < 200ms (30-day window)\nSLA: 99.9% uptime guaranteed (breach = credits)\n\nError budget = 1 - SLO = 0.1% = 43.8 min/month\n```\n\n## PromQL Essentials\n```promql\n# Request rate\nrate(http_requests_total[5m])\n\n# p99 latency\nhistogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m]))\n\n# Error rate %\nrate(http_requests_total{status=~\"5..\"}[5m])\n/ rate(http_requests_total[5m]) * 100\n```\n\n## OpenTelemetry Quick Setup\n```python\nfrom opentelemetry.instrumentation.fastapi import FastAPIInstrumentor\nFastAPIInstrumentor.instrument_app(app)\n# Auto-instruments all routes with traces\n```\n\n## Alerting Rules\n- Alert on SYMPTOMS (latency, errors) not CAUSES (CPU, memory)\n- Fast burn rate → page immediately\n- Slow burn rate → ticket for business hours\n- No SLO breach → dashboard/weekly review only\n\n## ELK Stack\n- Elasticsearch: stores + indexes logs\n- Logstash / Filebeat: ships logs from services\n- Kibana: dashboards + log search\n\n## Structured Logging (always JSON)\n```python\nlog.info(\"event\", user_id=uid, duration_ms=142, trace_id=tid)\n```",
    "resources": [
      {
        "title": "Site Reliability Engineering (SRE Book)",
        "author": "Google",
        "category": "books",
        "justification": "Chapters on SLIs/SLOs/SLAs and the four golden signals are the canonical reference",
        "bestFor": "Engineers learning SRE principles",
        "estimatedTime": "15 hours",
        "cost": "Free online",
        "confidence": "HIGH",
        "url": "https://sre.google/sre-book/table-of-contents/"
      },
      {
        "title": "Prometheus Documentation",
        "author": "CNCF",
        "category": "docs",
        "justification": "Official Prometheus docs covering data model, PromQL, and alerting",
        "bestFor": "Engineers instrumenting services with Prometheus",
        "estimatedTime": "4 hours",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://prometheus.io/docs/introduction/overview/"
      },
      {
        "title": "OpenTelemetry Documentation",
        "author": "CNCF",
        "category": "docs",
        "justification": "Official OTel getting started and SDK reference for multiple languages",
        "bestFor": "Engineers adding distributed tracing",
        "estimatedTime": "3 hours",
        "cost": "Free",
        "confidence": "HIGH",
        "url": "https://opentelemetry.io/docs/"
      },
      {
        "title": "Observability Engineering",
        "author": "Majors, Fong-Jones, Miranda",
        "category": "books",
        "justification": "Modern observability engineering with structured events, distributed tracing, and high-cardinality analysis",
        "bestFor": "Senior engineers building observability platforms",
        "estimatedTime": "12 hours",
        "cost": "~$40",
        "confidence": "HIGH"
      }
    ],
    "ladder": {
      "levels": [
        {
          "level": 1,
          "name": "Novice",
          "dreyfusLabel": "Novice",
          "description": "Understands what monitoring is and why it matters",
          "observableSkills": [
            "Knows difference between metrics, logs, and traces",
            "Can read a Grafana dashboard",
            "Understands uptime/availability concepts"
          ],
          "milestoneProject": {
            "title": "Basic Health Dashboard",
            "description": "Instrument a simple web app with Prometheus metrics; create a Grafana dashboard showing request rate and error rate",
            "estimatedHours": 4
          },
          "commonPlateaus": [
            "Confusing monitoring with observability",
            "Alert fatigue from threshold-based alerts"
          ],
          "estimatedHours": 6,
          "prerequisites": [
            "Basic Linux familiarity",
            "One programming language"
          ]
        },
        {
          "level": 2,
          "name": "Advanced Beginner",
          "dreyfusLabel": "Advanced Beginner",
          "description": "Instruments services and writes meaningful alerts",
          "observableSkills": [
            "Instruments HTTP services with golden signal metrics",
            "Writes PromQL queries for rate and histogram_quantile",
            "Creates structured logs with trace_id correlation"
          ],
          "milestoneProject": {
            "title": "Golden Signals Dashboard + Alerts",
            "description": "Instrument a 2-service system with Prometheus, create Grafana dashboards for all four golden signals, add alerting rules for error rate and latency",
            "estimatedHours": 12
          },
          "commonPlateaus": [
            "Alerting on causes instead of symptoms",
            "Not using structured logging"
          ],
          "estimatedHours": 25,
          "prerequisites": [
            "Docker/Kubernetes basics",
            "Basic Prometheus familiarity"
          ]
        },
        {
          "level": 3,
          "name": "Competent",
          "dreyfusLabel": "Competent",
          "description": "Implements distributed tracing and designs SLOs for production services",
          "observableSkills": [
            "Adds distributed tracing with OTel across multiple services",
            "Defines SLIs and SLOs with error budgets",
            "Implements log correlation with trace IDs"
          ],
          "milestoneProject": {
            "title": "Full Observability Stack",
            "description": "Set up Prometheus + Grafana + Jaeger with OTel for a 3-service application; define SLOs with error budget dashboards",
            "estimatedHours": 25
          },
          "commonPlateaus": [
            "Trace context propagation breaking at service boundaries",
            "SLO too aggressive burning budget too fast"
          ],
          "estimatedHours": 50,
          "prerequisites": [
            "Prometheus + Grafana experience",
            "Microservice architecture understanding"
          ]
        },
        {
          "level": 4,
          "name": "Proficient",
          "dreyfusLabel": "Proficient",
          "description": "Designs observability strategy for large distributed systems",
          "observableSkills": [
            "Implements burn rate alerting based on SLO consumption",
            "Designs multi-service tracing with sampling strategy",
            "Optimises observability cost at scale (log sampling, metric cardinality)"
          ],
          "milestoneProject": {
            "title": "SLO-Based Alerting Platform",
            "description": "Implement error budget burn rate alerting for a 10-service system; design a sampling strategy that retains 100% of error traces while sampling 1% of success traces",
            "estimatedHours": 40
          },
          "commonPlateaus": [
            "Observability cost explosion from high-cardinality labels",
            "Sampling discarding important error traces"
          ],
          "estimatedHours": 80,
          "prerequisites": [
            "Production SRE/monitoring experience",
            "PromQL proficiency"
          ]
        },
        {
          "level": 5,
          "name": "Expert",
          "dreyfusLabel": "Expert",
          "description": "Architects observability platforms for internet-scale systems",
          "observableSkills": [
            "Designs multi-region observability with global dashboards",
            "Evaluates build-vs-buy for observability infrastructure",
            "Defines SLOs across complex multi-party service dependencies"
          ],
          "milestoneProject": {
            "title": "Enterprise Observability Platform",
            "description": "Design a unified observability platform for 100+ microservices across 3 regions with SLO tracking, cost management, and on-call workflow integration",
            "estimatedHours": 100
          },
          "commonPlateaus": [
            "Observability infrastructure itself becoming a reliability risk",
            "Cultural resistance to SLO ownership"
          ],
          "estimatedHours": 200,
          "prerequisites": [
            "Senior SRE or platform engineering experience",
            "Large-scale distributed systems experience"
          ]
        }
      ]
    },
    "plan": {
      "overview": "Master monitoring and observability from metrics basics to production-grade distributed tracing and SLO-based alerting, covering Prometheus, ELK, OpenTelemetry, and Google SRE patterns.",
      "skippedTopics": "Specific APM vendor products (Datadog, New Relic) configuration details, chaos engineering, AIOps/anomaly detection",
      "sessions": [
        {
          "sessionNumber": 1,
          "title": "The Three Pillars, Golden Signals, and SLI/SLO/SLA",
          "paretoJustification": "SLOs and the four golden signals form the foundation of every SRE interview and production monitoring discussion.",
          "difficulty": "intermediate",
          "estimatedMinutes": 50,
          "hookQuestion": "Your service has been having intermittent issues for 3 weeks. Users complain, but your CPU and memory dashboards look normal. What are you measuring wrong?",
          "realWorldMotivation": "Poor observability is the root cause of most extended production incidents. Engineers who understand SLOs and golden signals can detect, diagnose, and resolve incidents faster and more reliably.",
          "objectives": [
            "Explain the three pillars of observability and what each answers",
            "List the four golden signals and write PromQL queries for each",
            "Define SLI, SLO, and SLA with concrete examples",
            "Calculate error budget from an SLO and explain how it guides engineering decisions"
          ],
          "content": "## The Three Pillars of Observability\n\n**Observability** is the ability to understand the internal state of a system from its external outputs. The three pillars:\n\n```mermaid\ngraph LR\n    M[Metrics<br/>What is happening<br/>Prometheus/Grafana]\n    L[Logs<br/>What happened<br/>ELK Stack]\n    T[Traces<br/>Where it happened<br/>Jaeger/Zipkin]\n\n    M -->|alert on anomalies| A[On-call Engineer]\n    L -->|investigate cause| A\n    T -->|find root service| A\n```\n\n- **Metrics**: Numeric measurements over time (CPU %, request rate, error rate). Cheap to store, easy to alert on.\n- **Logs**: Timestamped records of events (structured JSON preferred). Rich context, expensive at scale.\n- **Traces**: End-to-end request tracking across services with timing. Essential for microservices.\n\n---\n\n## The Four Golden Signals (Google SRE)\n\nGoogle's Site Reliability Engineering book defines four signals that, if measured well, cover most production issues:\n\n| Signal | Description | Example Metric |\n|---|---|---|\n| **Latency** | Time to serve a request | p99 response time: 250ms |\n| **Traffic** | Demand on the system | HTTP requests per second: 5,000 |\n| **Errors** | Rate of failed requests | 5xx error rate: 0.1% |\n| **Saturation** | How full the service is | CPU: 78%, queue depth: 1,200 |\n\n**Why these four?** They directly answer: is the system slow? under load? failing? about to fall over?\n\n---\n\n## SLI, SLO, and SLA\n\n```\nSLA (contract) → SLO (internal target) → SLI (measurement)\n```\n\n- **SLI (Service Level Indicator)**: The actual measurement. \"What fraction of requests complete in <200ms?\"\n- **SLO (Service Level Objective)**: The target. \"99% of requests complete in <200ms over 30 days.\"\n- **SLA (Service Level Agreement)**: The contractual commitment with consequences. \"We guarantee 99.9% availability. Breach = credits.\"\n\n**Error budget:**\n```\nError budget = 1 - SLO\n99.9% SLO → 0.1% error budget\n= 43.8 minutes downtime per month allowed\n= 8.76 hours per year\n```\n\nIf you burn through your error budget, you stop feature development and focus on reliability.\n\n---\n\n## Prometheus + Grafana Stack\n\n**Prometheus** scrapes metrics from instrumented services on a pull model. Stores as time-series data.\n\n```yaml\n# prometheus.yml — scrape configuration\nscrape_configs:\n  - job_name: 'api-service'\n    scrape_interval: 15s\n    static_configs:\n      - targets: ['api:8080']  # Prometheus scrapes /metrics endpoint\n```\n\n**Instrumenting a service (Python):**\n```python\nfrom prometheus_client import Counter, Histogram, start_http_server\n\nREQUEST_COUNT = Counter('http_requests_total', 'Total requests', ['method', 'endpoint', 'status'])\nREQUEST_LATENCY = Histogram('http_request_duration_seconds', 'Request latency',\n                             buckets=[0.01, 0.05, 0.1, 0.25, 0.5, 1.0, 2.5, 5.0])\n\n@app.middleware(\"http\")\nasync def metrics_middleware(request, call_next):\n    start = time.time()\n    response = await call_next(request)\n    duration = time.time() - start\n    REQUEST_COUNT.labels(request.method, request.url.path, response.status_code).inc()\n    REQUEST_LATENCY.observe(duration)\n    return response\n```\n\n**PromQL — querying Prometheus:**\n```promql\n# Request rate over last 5 minutes\nrate(http_requests_total[5m])\n\n# 99th percentile latency\nhistogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m]))\n\n# Error rate as percentage\nrate(http_requests_total{status=~\"5..\"}[5m]) / rate(http_requests_total[5m]) * 100\n\n# Alert: error rate > 1%\nALERT HighErrorRate\n  IF rate(http_requests_total{status=~\"5..\"}[5m]) / rate(http_requests_total[5m]) > 0.01\n  FOR 5m\n  LABELS { severity = \"critical\" }\n```\n\n---\n\n## ELK Stack: Elasticsearch + Logstash + Kibana\n\n- **Elasticsearch**: Distributed search and analytics engine — stores and indexes logs\n- **Logstash**: Data ingestion pipeline — collects, transforms, ships logs\n- **Kibana**: Visualisation layer — dashboards, log exploration, alerting\n\n**Modern alternative:** Elasticsearch + Filebeat (lightweight log shipper) + Kibana (drop Logstash for simple pipelines)\n\n**Structured logging (always use JSON):**\n```python\nimport structlog\n\nlog = structlog.get_logger()\n\nlog.info(\"request_completed\",\n         user_id=\"user_123\",\n         endpoint=\"/api/checkout\",\n         duration_ms=142,\n         status=200,\n         trace_id=\"abc-123-def\")\n```\n\nStructured logs are machine-parseable, filterable, and can be correlated with traces via `trace_id`.",
          "keyTakeaways": [
            "Metrics/Logs/Traces each answer a different question — use all three together",
            "Alert on the four golden signals (latency, traffic, errors, saturation), not on infrastructure causes",
            "SLI is the measurement, SLO is the target, SLA is the contract",
            "Error budget = 1 - SLO; consuming it stops features, forces reliability work",
            "Structured JSON logs are essential — unstructured logs cannot be queried or correlated with traces"
          ],
          "activities": [
            {
              "description": "Instrument a simple HTTP server with Prometheus metrics for all four golden signals",
              "durationMinutes": 20
            },
            {
              "description": "Define SLI and SLO for a hypothetical payments API; calculate its monthly error budget in minutes",
              "durationMinutes": 10
            },
            {
              "description": "Write PromQL queries for error rate and p99 latency",
              "durationMinutes": 10
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 10
            }
          ],
          "resources": [
            {
              "title": "Google SRE Book — SLOs chapter",
              "type": "article",
              "url": "https://sre.google/sre-book/service-level-objectives/"
            },
            {
              "title": "Prometheus getting started",
              "type": "docs",
              "url": "https://prometheus.io/docs/prometheus/latest/getting_started/"
            }
          ],
          "reviewQuestions": [
            "What are the three pillars of observability and what question does each answer?:::Metrics: 'what is happening' — numeric measurements over time, cheap to store, easy to alert on. Logs: 'what happened' — timestamped event records with rich context. Traces: 'where it happened' — end-to-end request tracking across services showing which service is slow.",
            "What are the four golden signals and why were they chosen?:::Latency (how slow?), Traffic (how much load?), Errors (how many failures?), Saturation (how full?). They were chosen because together they answer the most important operational questions about a service's health and capacity.",
            "Define SLI, SLO, and SLA and give an example of each for a web API.:::SLI: actual measurement — 'fraction of requests completing in <200ms'. SLO: internal target — '99% of requests complete in <200ms over 30 days'. SLA: contractual commitment — 'we guarantee 99.9% uptime; breach triggers 25% service credit'.",
            "What is an error budget and how does it guide engineering decisions?:::Error budget = 1 - SLO. A 99.9% SLO gives 0.1% error budget = 43.8 minutes downtime/month. If the error budget is consumed, teams stop shipping new features and focus on reliability work. It turns an abstract SLO into a concrete resource that balances reliability vs feature velocity."
          ],
          "successCriteria": "You can list the four golden signals, write PromQL for each, define SLI/SLO/SLA with examples, and calculate error budget from an SLO target."
        },
        {
          "sessionNumber": 2,
          "title": "OpenTelemetry, Distributed Tracing, and Production Alerting Strategy",
          "paretoJustification": "Distributed tracing and SLO-based alerting are the most advanced observability skills and appear in senior-level system design discussions.",
          "difficulty": "advanced",
          "estimatedMinutes": 55,
          "hookQuestion": "A checkout request takes 2 seconds. Your API gateway logs show the request came in and a response went out. But which of your 8 downstream services caused the 2-second delay?",
          "realWorldMotivation": "In a microservice architecture, a single user request touches dozens of services. Without distributed tracing, diagnosing latency is guesswork. OpenTelemetry is now the industry standard for solving this.",
          "objectives": [
            "Instrument a multi-service application with OpenTelemetry for distributed tracing",
            "Explain trace context propagation and why it is essential",
            "Design an alerting strategy using error budget burn rates",
            "Architect a complete observability stack for a production system"
          ],
          "content": "## OpenTelemetry: Unified Observability\n\nOpenTelemetry (OTel) is the CNCF standard for instrumenting applications. It provides a single SDK that exports metrics, logs, and traces to any backend (Jaeger, Prometheus, Datadog, etc.).\n\n```mermaid\ngraph LR\n    subgraph App\n        SDK[OTel SDK]\n    end\n    SDK -->|OTLP| COL[OTel Collector]\n    COL -->|metrics| PROM[Prometheus]\n    COL -->|traces| JAEGER[Jaeger]\n    COL -->|logs| ES[Elasticsearch]\n```\n\n**Tracing a FastAPI application:**\n```python\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import BatchSpanProcessor\nfrom opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter\nfrom opentelemetry.instrumentation.fastapi import FastAPIInstrumentor\n\n# Configure tracing\nprovider = TracerProvider()\nexporter = OTLPSpanExporter(endpoint=\"http://otel-collector:4317\")\nprovider.add_span_processor(BatchSpanProcessor(exporter))\ntrace.set_tracer_provider(provider)\n\n# Auto-instrument FastAPI (adds spans for every request)\nFastAPIInstrumentor.instrument_app(app)\n\n# Manual spans for custom operations\ntracer = trace.get_tracer(__name__)\n\nasync def process_payment(order_id: str, amount: float):\n    with tracer.start_as_current_span(\"process_payment\") as span:\n        span.set_attribute(\"order.id\", order_id)\n        span.set_attribute(\"payment.amount\", amount)\n        try:\n            result = await payment_gateway.charge(amount)\n            span.set_attribute(\"payment.status\", \"success\")\n            return result\n        except Exception as e:\n            span.record_exception(e)\n            span.set_status(trace.StatusCode.ERROR, str(e))\n            raise\n```\n\n**Trace context propagation** — the key to distributed tracing:\n```python\n# Outgoing HTTP call — propagate trace context\nimport httpx\nfrom opentelemetry.propagate import inject\n\nheaders = {}\ninject(headers)  # adds traceparent, tracestate headers\nresponse = await httpx.AsyncClient().get(\"http://downstream-service/api\", headers=headers)\n```\n\nThis lets Jaeger stitch together a full distributed trace from multiple services.\n\n---\n\n## Alerting Strategy: Alert on Symptoms, Not Causes\n\n**Bad alerts (cause-based):**\n- CPU > 80% (might not affect users)\n- Disk > 90% (might be normal for a logging server)\n- Memory > 85% (JVMs run high normally)\n\n**Good alerts (symptom-based, using golden signals):**\n```yaml\n# Prometheus alerting rules\ngroups:\n  - name: golden-signals\n    rules:\n      - alert: HighLatency\n        expr: histogram_quantile(0.99, rate(http_request_duration_seconds_bucket[5m])) > 1.0\n        for: 5m\n        annotations:\n          summary: \"p99 latency >1s for 5 minutes\"\n\n      - alert: HighErrorRate\n        expr: rate(http_requests_total{status=~\"5..\"}[5m]) / rate(http_requests_total[5m]) > 0.01\n        for: 2m\n        annotations:\n          summary: \"Error rate >1% for 2 minutes\"\n\n      - alert: ServiceDown\n        expr: up{job=\"api-service\"} == 0\n        for: 1m\n        annotations:\n          summary: \"API service scrape target is down\"\n```\n\n**Alerting tiers:**\n1. **Page immediately**: SLO breach imminent (error budget burning fast)\n2. **Ticket**: Slow error budget burn (can wait until business hours)\n3. **Dashboard/review**: Informational — review weekly\n\n---\n\n## Distributed Tracing Deep Dive\n\n```mermaid\ngantt\n    dateFormat  SSS\n    axisFormat %Lms\n    title Request trace: POST /checkout\n\n    section API Gateway\n    auth-check      :0, 5\n    route           :5, 2\n\n    section Order Service\n    validate-cart   :7, 15\n    create-order    :22, 8\n\n    section Payment Service\n    charge-card     :30, 120\n\n    section Inventory Service\n    reserve-items   :30, 45\n\n    section Email Service\n    send-confirm    :150, 20\n```\n\nA trace shows the critical path: Payment Service at 120ms is the bottleneck, not Email Service (parallel).\n\n**Key trace concepts:**\n- **Trace**: One end-to-end request across services (has a `trace_id`)\n- **Span**: One unit of work within a trace (has `span_id`, `parent_span_id`, start/end time)\n- **Context propagation**: Passing `traceparent` header between services to link spans\n\n---\n\n## Observability in System Design Interviews\n\nWhen designing any system, proactively address observability:\n\n**\"How would you monitor this?\"**\n\nStructure your answer around:\n\n1. **Golden signal metrics**: What to measure for each service\n2. **SLOs**: What availability/latency targets, what error budget\n3. **Alerting**: Alert on SLO burn rate, not individual metrics\n4. **Dashboards**: Per-service golden signals + business metrics side-by-side\n5. **Runbooks**: For each alert, document diagnosis steps\n\n**Error Budget Burn Rate Alerting (most sophisticated pattern):**\n```promql\n# Fast burn: consuming 2% of monthly budget in 1h → page immediately\n(rate(http_requests_total{status=~\"5..\"}[1h]) / rate(http_requests_total[1h])) > 0.02\n\n# Slow burn: consuming budget faster than allowed over 6h → ticket\n(rate(http_requests_total{status=~\"5..\"}[6h]) / rate(http_requests_total[6h])) > 0.005\n```\n\nThis is the Google SRE recommended alerting pattern: alert on the rate of SLO consumption, not the raw metric value.",
          "keyTakeaways": [
            "OpenTelemetry is vendor-neutral — instrument once, export to any backend",
            "Context propagation (traceparent header) links spans across services into a single trace",
            "Alert on symptoms (error rate, latency) not causes (CPU, disk); alert on SLO burn rate not raw values",
            "Fast burn rate (paging) + slow burn rate (ticket) covers both acute and gradual degradation",
            "Traces reveal the critical path — the slowest sequential operation, not just the slowest service"
          ],
          "activities": [
            {
              "description": "Add OTel tracing to a 2-service application; verify traces appear in Jaeger UI",
              "durationMinutes": 25
            },
            {
              "description": "Write Prometheus alerting rules for fast and slow SLO burn rates",
              "durationMinutes": 15
            },
            {
              "description": "Answer review questions",
              "durationMinutes": 15
            }
          ],
          "resources": [
            {
              "title": "OpenTelemetry Python docs",
              "type": "docs",
              "url": "https://opentelemetry.io/docs/languages/python/"
            },
            {
              "title": "Google SRE — Alerting on SLOs",
              "type": "article",
              "url": "https://sre.google/workbook/alerting-on-slos/"
            }
          ],
          "reviewQuestions": [
            "What is OpenTelemetry and why is it preferred over vendor-specific instrumentation?:::OpenTelemetry is the CNCF standard SDK for metrics, logs, and traces. It is vendor-neutral — instrument once with OTel and export to any backend (Prometheus, Jaeger, Datadog, Honeycomb). Vendor-specific SDKs create lock-in: switching observability vendors requires re-instrumenting the entire application.",
            "What is context propagation in distributed tracing and why is it critical?:::Context propagation passes the trace ID and span ID between services via HTTP headers (traceparent header per W3C standard). Without it, each service creates an isolated trace and you cannot see the full picture of a cross-service request. With it, Jaeger/Zipkin stitches all spans into a single trace showing the complete request path and timing.",
            "What is the difference between alerting on causes (CPU %) vs symptoms (error rate)?:::Cause-based alerts (high CPU) fire often but do not indicate user impact — high CPU might be normal. Symptom-based alerts (high error rate, high latency) directly measure user experience. Alert on symptoms to reduce alert fatigue and ensure every alert represents a real user-visible problem.",
            "Explain error budget burn rate alerting and why it is better than threshold-based alerting.:::Burn rate alerting measures how fast you are consuming your error budget, not just whether a metric crossed a threshold. Fast burn (2% of monthly budget in 1 hour) pages immediately — the service will breach SLO soon. Slow burn (budget being consumed at 5× normal rate over 6 hours) creates a ticket for business hours. This prevents both under-alerting (missing slow degradation) and over-alerting (paging on transient spikes)."
          ],
          "successCriteria": "You can instrument a multi-service application with OTel, explain trace context propagation, and design an SLO-based alerting strategy with fast and slow burn rates."
        }
      ]
    },
    "quizBank": [
      {
        "question": "What are the four golden signals defined by Google SRE?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Remember",
        "options": [
          "CPU, Memory, Disk, Network",
          "Latency, Traffic, Errors, Saturation",
          "Availability, Reliability, Performance, Capacity",
          "P50, P95, P99, P999"
        ],
        "correctAnswer": "Latency, Traffic, Errors, Saturation",
        "explanation": "The four golden signals from Google's SRE book: Latency (how slow), Traffic (how much demand), Errors (how many failures), Saturation (how full). They directly measure user-visible service health."
      },
      {
        "question": "A service has a 99.9% availability SLO. How many minutes of downtime are allowed per month?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Apply",
        "options": [
          "1 minute",
          "43.8 minutes",
          "4.38 hours",
          "87.6 minutes"
        ],
        "correctAnswer": "43.8 minutes",
        "explanation": "Error budget = 1 - 0.999 = 0.1%. Month = 43,800 minutes. 0.001 × 43,800 = 43.8 minutes. This is the allowed downtime budget for the month."
      },
      {
        "question": "What is the difference between an SLI, SLO, and SLA?",
        "format": "free-response",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "SLI (Service Level Indicator): the actual measurement — e.g., 'fraction of requests completing in <200ms'. SLO (Service Level Objective): the internal target — '99% of requests complete in <200ms over 30 days'. SLA (Service Level Agreement): the contractual commitment with consequences — '99.9% availability guaranteed; breach triggers 25% service credit'.",
        "explanation": "SLIs are what you measure, SLOs are what you target internally, SLAs are what you promise externally with contractual consequences."
      },
      {
        "question": "What is OpenTelemetry and why is it preferred over vendor-specific monitoring SDKs?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Understand",
        "options": [
          "A paid observability platform by Google",
          "A vendor-neutral CNCF standard for metrics, logs, and traces that exports to any backend",
          "A replacement for Prometheus that uses push instead of pull",
          "A Kubernetes-specific monitoring solution"
        ],
        "correctAnswer": "A vendor-neutral CNCF standard for metrics, logs, and traces that exports to any backend",
        "explanation": "OpenTelemetry is vendor-neutral: instrument your application once with the OTel SDK and export to Prometheus, Jaeger, Datadog, Honeycomb, or any OTLP-compatible backend. Switching vendors does not require re-instrumenting your code."
      },
      {
        "question": "What is trace context propagation and why is it essential for distributed tracing?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Understand",
        "options": [],
        "correctAnswer": "Context propagation passes the trace ID and parent span ID between services via HTTP headers (W3C traceparent header standard). Without it, each service generates an isolated trace with no connection to the others — you cannot see the full picture of a cross-service request. With it, a distributed tracing system (Jaeger, Zipkin) stitches all spans into a single trace showing the complete request path, timing, and which service caused latency.",
        "explanation": "The traceparent header format: 00-{trace_id}-{parent_span_id}-{flags}. OpenTelemetry's propagators handle injection and extraction automatically."
      },
      {
        "question": "Why should you alert on symptoms (error rate, latency) rather than causes (CPU usage, memory)?",
        "format": "multiple-choice",
        "difficulty": 3,
        "bloomLabel": "Evaluate",
        "options": [
          "Symptom alerts are easier to configure in Prometheus",
          "Cause metrics like CPU do not have meaningful thresholds — symptoms directly measure user experience",
          "Infrastructure metrics cannot be collected without an agent",
          "Cause-based alerts fire too rarely"
        ],
        "correctAnswer": "Cause metrics like CPU do not have meaningful thresholds — symptoms directly measure user experience",
        "explanation": "High CPU might be normal (batch processing). High memory might be expected (JVM). But high error rate always means users are failing. Alert on what users experience, not internal resource usage. This reduces alert fatigue and ensures every alert represents a real problem."
      },
      {
        "question": "What is error budget burn rate alerting and how does it improve on simple threshold alerts?",
        "format": "free-response",
        "difficulty": 4,
        "bloomLabel": "Evaluate",
        "options": [],
        "correctAnswer": "Burn rate alerting measures how fast the monthly error budget is being consumed. A fast burn (e.g., 2% of monthly budget consumed in 1 hour) triggers an immediate page — the SLO will be breached soon. A slow burn (consuming budget 5x faster than sustainable over 6 hours) creates a ticket for business hours. Simple threshold alerts (error rate > 1%) miss gradual degradation and generate false positives on transient spikes. Burn rate alerts detect both acute and gradual SLO risk.",
        "explanation": "This is the Google SRE recommended alerting pattern described in the SRE Workbook chapter 'Alerting on SLOs'."
      },
      {
        "question": "What is the difference between metrics, logs, and traces in an observability system?",
        "format": "multiple-choice",
        "difficulty": 2,
        "bloomLabel": "Understand",
        "options": [
          "They are three names for the same thing — timestamped data",
          "Metrics are numeric aggregates over time; logs are timestamped event records; traces track a request end-to-end across services",
          "Metrics are for errors; logs are for performance; traces are for availability",
          "Logs replace metrics in modern observability; traces are only for databases"
        ],
        "correctAnswer": "Metrics are numeric aggregates over time; logs are timestamped event records; traces track a request end-to-end across services",
        "explanation": "Each pillar answers a different question: metrics (what is happening now, cheap to store), logs (what exactly happened with full context, expensive at scale), traces (which service in the call chain is slow or failing, essential for microservices)."
      }
    ]
  }
]